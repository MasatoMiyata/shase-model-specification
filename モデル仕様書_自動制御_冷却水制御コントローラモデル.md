# 冷却水制御モデル

## 1.  基本情報

| 項目 | 内容 |
|---|---|
| モデルID | CWC_001（モデルを一意に識別するためのID） |
| モデルの名称 | 冷却水制御モデル |
| モデルの概要 | 本モデルは、冷却水系統における制御ロジックを離散時間で表現する制御モデルである。冷却水出口温度を主制御対象とし、冷却塔ファン制御、三方弁バイパス制御、水質管理、凍結防止制御に基づき、ファン出力、弁開度、ブローダウン信号、ヒータ信号を算出する。 |
| カテゴリ | 制御 - 熱源 - 熱源補機（冷却水系統制御） |
| 対象機器 | 冷却塔ファン、冷却水三方弁、ブローダウン弁、凍結防止ヒータ |
| 制御方式 | ON/OFF制御、極数変換モータ（段階）制御、可変速（インバータ）制御、PI制御 |
| 制御対象 | 冷却水出口温度、水質（導電率）、凍結防止条件 |
| 参考規格・指針 | ASHRAE Guideline 36、EnergyPlus EMS/Setpoint 制御概念、Modelica Buildings Library の制御ブロック（参考） |
| モデル種別 | 離散時間制御モデル |
| 作成日 | 2026-07-06 |
| 作成者 | SHINRYO Yajima |
| バージョン情報 | 1.0.0 |
| ライセンス情報 | 使用許諾に関する情報（例: MITライセンス、クリエイティブ・コモンズなど） |

## 2.  入出力インターフェイス

### 2.1 入力項目

| 変数名 | 単位 | データ型 | 定義 | 備考 |
|---|---|---|---|---|
| `MODE` | \- | Integer | 運転モード | 0:停止、1:運転 |
| `T_cw_out` | ℃ | Real | 冷却水出口温度 | 主制御対象 |
| `T_cw_set` | ℃ | Real | 冷却水出口温度設定値 | 上位制御または手動設定 |
| `T_wb` | ℃ | Real | 外気湿球温度 | 凍結防止・外乱 |
| `COND` | mS/cm | Real | 冷却水導電率 | 水質制御用 |
| `LEVEL` | \- | Boolean | 水位信号 | 0:異常、1:正常 |
| `dt` | s | Real | サンプリング周期 | 一定周期を前提 |

### 2.2 出力項目

| 変数名 | 単位 | データ型 | 定義 |
|---|---|---|---|
| `STATUS` | \- | Integer | 運転状態（0:停止、1:正常、-1:異常、2:警告継続） |
| `FAN_STATUS` | \- | Integer | ファン運転状態（0:停止、1:運転） |
| `FAN_SPEED` | \- | Real | ファン速度比（0～1） |
| `FAN_STAGE` | \- | Integer | ファン段数 |
| `VALVE` | % | Real | 三方弁開度（0～100） |
| `BLOWDOWN` | \- | Boolean | ブローダウン弁信号 |
| `HEATER` | \- | Boolean | 凍結防止ヒータ信号 |
| `ERR_T` | ℃ | Real | 温度偏差（T_cw_out - T_cw_set） |

### 2.3 パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|---|---|---|---|
| `control_mode` | \- | \- | ファン制御方式（onoff, stage, VFD） |
| `Kp` | 1/℃ | Real | 比例ゲイン |
| `Ki` | 1/(℃・s) | Real | 積分ゲイン |
| `deadband` | ℃ | Real | 不感帯幅 |
| `T_on` | ℃ | Real | ON閾値 |
| `T_off` | ℃ | Real | OFF閾値 |
| `fan_min` | \- | Real | 最小ファン速度 |
| `fan_max` | \- | Real | 最大ファン速度 |
| `N_stage` | \- | Integer | 最大段数 |
| `COND_high` | mS/cm | Real | ブローダウン開始上限 |
| `COND_low` | mS/cm | Real | ブローダウン停止下限 |
| `T_freeze` | ℃ | Real | 凍結判定温度 |
| `alpha_f` | \- | Real | 温度フィルタ係数（任意） |

## 3.  計算ロジック

### 3.1 運転モード判定

``` text
if MODE == 0:
    FAN_STATUS = 0
    FAN_SPEED = 0
    FAN_STAGE = 0
    VALVE = 0
    BLOWDOWN = 0
    HEATER = 0
    ERR_T = 0
    STATUS = 0
    return
```

### 3.2 入力値チェック

以下の条件に該当する場合は異常停止または警告とする。

``` text
if dt <= 0:
    STATUS = -1
    stop
if T_cw_out is undefined or T_cw_set is undefined:
    STATUS = -1
    stop
if COND_low >= COND_high:
    STATUS = -1
    stop
if LEVEL == 0:
    STATUS = 2
```

### 3.3 偏差算出

温度偏差は次式で定義する。

``` math
e = T_cw_out - T_cw_set
```

必要に応じて温度入力は一次フィルタで平滑化する。

``` math
T_f[k] = alpha_f T_cw_out[k] + (1 - alpha_f) T_f[k-1]
```

### 3.4 ファン制御

#### 3.4.1 ON/OFF制御

``` text
if e >= T_on:
    FAN_STATUS = 1
elif e <= -T_off:
    FAN_STATUS = 0
else:
    FAN_STATUS = FAN_STATUS_prev

FAN_SPEED = 1.0 if FAN_STATUS == 1 else 0.0
```

#### 3.4.2 PI制御（VFD）

PI制御では、積分項と比例項からファン速度を算出する。

``` math
I[k] = I[k-1] + e[k] dt
```

``` math
u[k] = Kp e[k] + Ki I[k]
```

``` math
FAN_SPEED = min(fan_max, max(fan_min, u[k]))
```

アンチワインドアップを適用する場合は、出力飽和時に積分項更新を停止または補正する。

#### 3.4.3 段階制御（極数変換）

PI制御または比例出力を段階化して用いる。

``` math
FAN_STAGE = ceil(N_stage × FAN_SPEED)
```

``` math
FAN_STAGE = min(max(FAN_STAGE, 0), N_stage)
```

control_mode = stage の場合、ファン速度は次式とする。

``` math
FAN_SPEED = FAN_STAGE / N_stage
```

#### 3.4.4 発停との整合

``` text
if FAN_SPEED <= 0:
    FAN_STATUS = 0
else:
    FAN_STATUS = 1
```

### 3.5 三方弁バイパス制御

冷凍機入口下限温度を下回らないようにするため、冷却水出口温度が設定値を下回る場合にバイパス流量を増加させる。

``` text
if T_cw_out < T_cw_set - deadband:
    VALVE = 100
elif T_cw_out >= T_cw_set:
    VALVE = 0
else:
    VALVE = 100 * (T_cw_set - T_cw_out) / deadband
```

VALVE = 0 は全冷却塔流、VALVE = 100 は全バイパスを表す。

### 3.6 水質制御

導電率に基づくヒステリシス制御を用いる。

``` text
if COND >= COND_high:
    BLOWDOWN = 1
elif COND <= COND_low:
    BLOWDOWN = 0
else:
    BLOWDOWN = BLOWDOWN_prev
```

### 3.7 凍結防止制御

``` text
if T_wb <= T_freeze and LEVEL == 1:
    HEATER = 1
else:
    HEATER = 0
```

安全上、LEVEL == 0 の場合は HEATER = 0 とする。

### 3.8 全体アルゴリズム

``` text
1. 停止判定
    if MODE == 0:
        set all outputs to stopped values
        return
2. 入力チェック
    check dt, temperature signals, level signal, conductivity thresholds
3. 偏差計算
    e = T_cw_out - T_cw_set
4. ファン制御
    apply onoff / stage / pi according to control_mode
5. 三方弁制御
    calculate VALVE from T_cw_out and T_cw_set
6. 補助制御
    update BLOWDOWN and HEATER
7. 出力整合
    update FAN_STATUS, FAN_STAGE, STATUS
```

## 4.  適用範囲

### 4.1 冷却水制御モデルの適用対象

| 項目 | 単位 | 下限 | 上限 | 範囲外処理 |
|---|---|---|---|---|
| 冷却水出口温度 | ℃ | 5 | 40 | 範囲外は警告 |
| 冷却水出口温度設定値 | ℃ | 10 | 35 | 範囲外は入力エラー |
| 外気湿球温度 | ℃ | -5 | 35 | 低温側では凍結防止を優先 |
| 導電率 | mS/cm | 0 | 10 | 範囲外は警告または異常 |

### 4.2 前提条件

- 制御周期 dt が一定であること
- 実機とモデルで制御ロジックの条件分岐が一致していること
- 温度センサ精度が概ね ±0.5℃以内であること
- 制御対象は冷却塔付き冷却水系統であること

### 4.3 適用限界

- 適応制御、フィードフォワード制御などの高度制御は対象外
- 起動直後、停止直前、セル切替直後などの過渡状態では誤差が増大しうる

## 5.  制御別特性

### 5.1 ON/OFF制御

| 項目 | 内容 |
|---|---|
| 容量制御 | 閾値による発停制御 |
| 特徴 | 実装が容易で安定しやすいが、温度振幅が大きくなりやすい |
| 推奨用途 | 小規模設備、定速ファン |
| 注意点 | deadband が小さいとチャタリングを生じやすい |

### 5.2 段階制御

| 項目 | 内容 |
|---|---|
| 容量制御 | 複数セルまたは段数による離散制御 |
| 特徴 | ON/OFF より高分解能だが、段切替時の不連続がある |
| 推奨用途 | 複数セル冷却塔、インバータなし設備 |
| 注意点 | 段数が少ないと振動・オーバーシュートが生じやすい |

### 5.3 PI制御

| 項目 | 内容 |
|---|---|
| 容量制御 | 比例・積分演算による連続制御 |
| 特徴 | 高い追従性と省エネルギー性が期待できる |
| 推奨用途 | インバータファン、高精度制御 |
| 注意点 | Kp、Ki の設定不適切時に振動・遅れが発生する |

## 6.  精度・検証

| 項目 | 内容 |
|---|---|
| 検証方法 | BMS/DDC の実測制御信号とモデル出力の比較、状態遷移の一致確認 |
| 使用データ | T_cw_out、T_cw_set、T_wb、COND、LEVEL、FAN_SPEED、VALVE、FAN_STATUS |
| 評価指標 | RMSE、MAE、状態一致率、切替タイミング誤差 |
| 目標精度 | 連続出力 RMSE 10%以内、状態一致率 90%以上を目安 |

誤差指標は以下とする。

``` math
RMSE_u = \sqrt{\frac{1}{N} \sum_{k=1}^{N} \left( u_{model}[k] - u_{meas}[k] \right)^2}
```

``` math
MAE_u = \frac{1}{N} \sum_{k=1}^{N} \left| u_{model}[k] - u_{meas}[k] \right|
```

``` math
Match\_rate = \frac{N_{match}}{N_{total}}
```

## 7.  パラメータ同定

### 7.1 同定対象

| 対象 | 係数 | 説明 |
|---|---|---|
| PI制御ゲイン | `Kp`、`Ki` | 応答速度と定常偏差を決定 |
| ヒステリシス | `T_on`、`T_off`、`deadband` | 発停条件と安定性を決定 |
| 出力制約 | `fan_min`、`fan_max`、`N_stage` | ファン出力の物理制約を表現 |
| 補助制御 | `COND_high`、`COND_low`、`T_freeze` | 水質・凍結防止条件を表現 |

### 7.2 回帰・最適化の考え方

連続出力と状態遷移の両方を対象とする。

``` math
J = w_1 \sum \left( FAN\_SPEED_{model} - FAN\_SPEED_{meas} \right)^2
  + w_2 \sum \left( VALVE_{model} - VALVE_{meas} \right)^2
  + w_3 \sum \left| STATE_{model} - STATE_{meas} \right|
```

### 7.3 同定方法

- まずロジック一致を確認する
- 次に `T_on`、`T_off`、`deadband` など閾値を調整する
- 最後に `Kp`、`Ki` を調整して応答と振動を合わせる
- サンプリング周期の不一致がある場合は、同定前に再サンプリングする

## 8.  Cx活用

### 8.1 Cxにおける本モデルの位置づけ
本冷却水制御モデルは、冷却塔ファン（ON/OFF・段階・VFD）、バイパス弁／三方弁、冷却水温度設定（固定・外気湿球追従・最適化）等の**制御ロジックの妥当性を定量確認するための基準モデル**として活用する。
また、冷却水温度はターボ冷凍機のCOPに強く影響するため、**冷凍機モデル（機器側）と冷却水制御モデル（制御側）を接続**し、単体最適ではなく**熱源システム全体（冷凍機＋冷却塔＋ポンプ等）の最適性**でCxを行う。

Cx（コミッショニング）における主目的は以下である。

- 設計時に想定した制御方式・設定値（$T_{cw,set}$、deadband、段階切替閾値、VFD下限等）が妥当であることを確認する
- 試運転時に、実測の制御信号・温度応答とモデル出力を比較し、制御動作・遅れ・ハンチング・センサ整合性を確認する
- 運用時に、追従性低下、過制御、出力飽和、センサ異常、冷却塔性能劣化（汚れ、散水不良、充填材劣化等）を検知する
- 改修時に、制御パラメータ変更や運用変更（設定緩和、最適化、台数制御連携等）の省エネ効果・リスク（凍結、能力不足）を定量評価する

### 8.2 フェーズ別活用一覧（修正・拡張）

| フェーズ | 活用目的 | 活用内容 | 入力データ | 出力・判断 |
|---|---|---|---|---|
| 設計 | 制御方式・構成選定 | ON/OFF・段階・VFD、バイパス弁有無、台数制御連携の比較 | 外気湿球温度・外気条件、負荷プロファイル、機器仕様（ファン/ポンプ/塔性能）、制約条件（騒音・凍結） | 追従性、ハンチングリスク、費用対効果、運用難易度 |
| 設計 | 冷却水温度設定の妥当性 | $T_{cw,set}$（固定/季節/湿球追従/最適化）での電力最小化と安全側制約確認 | 湿球温度、冷凍機特性（COP vs $T_{cw,in}$）、冷却塔・ファン電力特性 | $P_{plant}$最小条件、設定値レンジ、運転下限/上限の整理 |
| 設計 | 制御パラメータ設計 | ゲイン、deadband、フィルタ、段階切替条件、VFD下限・上限、最小運転時間の設計 | 想定外乱（湿球変動、負荷変動）、通信周期、センサ仕様 | 過制御/遅れ/チャタリング抑制、調整指針 |
| 施工 | 試運転（制御動作）確認 | 実測制御信号とモデル出力を比較（ステップ試験、外乱応答） | BMSトレンド、設定値、運転状態、手動ステップ操作記録 | ロジック一致、遅れ、ハンチング、上限下限拘束の確認 |
| 施工 | 計測・入出力整合確認 | 温度・流量・信号の符号/スケール/単位、演算値の妥当性 | センサ校正記録、I/Oリスト、BMS演算式 | センサ異常候補、トレンド品質（欠損・分解能） |
| 運用 | 制御品質監視 | モデルとの差分（残差）から制御異常・性能劣化を検出 | 運転データ、外乱（$T_{wb}$等）、制御出力、アラーム履歴 | 過制御/遅れ/出力飽和、塔性能低下、センサ異常候補 |
| 運用 | 省エネ運用支援 | “冷却水を下げるほど良い”の単体最適を回避し、合計電力で最適化 | 冷凍機電力、冷却塔ファン電力、ポンプ電力、$T_{cw}$ | $P_{plant}$最小化、設定見直し余地、運用改善メニュー |
| 改修 | 制御改善効果評価 | パラメータ変更・最適化導入前後の応答/電力比較 | 改修前後BMS、気象、負荷 | 追従性改善、ハンチング低減、省エネルギー効果の定量化 |
| 改修 | M&V基準モデル | 改修前モデルをベースラインとして改修後実績と比較 | 改修前実績、改修後実績、気象・負荷条件 | 省エネルギー効果の定量化、リスク（能力/凍結）の説明性 |

### 8.3 設計フェーズでの活用
設計フェーズでは、冷却水制御を「追従性」だけでなく「熱源システム総合電力」で評価する。

#### 8.3.1 制御方式および冷却水温度設定の確認
冷却水温度を下げると冷凍機電力は下がる一方、冷却塔ファン電力は増加する。よって評価指標は、例えば以下を用いる。

``` math
P_{plant} = P_{chiller} + P_{ct,fan} + P_{cw,pump} \;(+ P_{others})
```

モデルにより、$T_{cw,set}$や制御方式変更時の $\Delta P_{plant}$ を比較し、**「冷凍機単体最適」ではなく「システム最小」**の条件を探索する。

#### 8.3.2 制御パラメータの確認
- $T_{cw,set}$ の季節切替／湿球追従時に、過度に低い設定となっていない（凍結・白煙・水質・騒音制約）
- VFD下限（最低回転数）やdeadbandが不適切で、微小偏差でハンチング・チャタリングを誘発しない
- 冷却塔台数制御（セル台数）とファン制御が整合
- 冷凍機台数制御と冷却塔台数制御の連携

### 8.4 施工・試運転フェーズでの活用
施工・試運転フェーズでは、**モデル出力と実測の時系列比較**により、制御ロジックの一致と応答品質を確認する。

- **ステップ試験**：$T_{cw,set}$ を一定幅変更し、$T_{cw,out}$ と操作量（ファン速度/段数/弁開度）の応答（遅れ、オーバーシュート、整定時間）を比較
- **外乱応答**：$T_{wb}$ 変動や負荷変動（冷凍機負荷、凝縮器負荷）に対する追従性を比較
- **拘束条件確認**：上限/下限（ファン上限、下限回転数、段階切替、最小ON時間）が意図通りに機能しているか確認

### 8.5 運用フェーズでの活用
運用中は、モデル値と実測値の差分を残差として定義し、異常候補を抽出する。

例として以下を定義する（用途に応じて選択）。

- 温度の残差
``` math
r_{T,cw} = T_{cw,out,meas} - T_{cw,out,model}
```

- 設定値追従誤差（モデルを使わずに監視する場合）
``` math
e_{cw} = T_{cw,out,meas} - T_{cw,set}
```

- 操作量（ファン速度等）の残差
``` math
r_{u} = u_{meas} - u_{model}
```

- 冷却塔ファン電力の残差（省エネ・機器劣化監視）
``` math
r_{P,fan} = P_{fan,meas} - P_{fan,model}
```

残差が継続的に大きい、または特定のパターン（振動、飽和、張り付き）を示す場合に異常候補を抽出する。

#### 8.5.1 異常検知の例

| 現象 | 代表的な兆候（例） | 想定される原因 |
|---|---|---|
| 制御遅れ | $u_{model} > u_{meas}$ が継続、または応答遅延が一定以上 | ゲイン不足、フィルタ過大、通信周期/遅延、I/O処理遅れ |
| 過制御（ハンチング） | $u$ や $T_{cw,out}$ の振動が大きい（周期的） | $K_p$過大、deadband不足、ノイズ増大、段階切替閾値不適切 |
| チャタリング（ON/OFF頻繁） | ファンON/OFFや段階切替が短周期で反復 | deadband不足、最小ON/OFF時間なし、閾値設計不良 |
| 出力飽和（上限張り付き） | $u \approx u_{max}$ なのに $e_{cw}>0$ が継続（温度が下がらない） | 冷却塔能力不足、散水不良、充填材劣化、外気条件悪化、流量不足、ストレーナ詰まり |
| 出力張り付き（変化しない） | $u$ が一定で変化しない／不連続 | センサ異常、信号断、制御停止、手動固定、BMS演算停止 |
| 温度計測の不整合 | $T_{cw,in}$ と $T_{cw,out}$ の関係が物理的に不自然、急変 | センサ断線/オフセット、取付不良、スケーリング誤り |
| 省エネルギー性能の悪化 | $P_{fan,meas}$ がモデルより過大、または $P_{plant}$が悪化 | VFD設定不良、ファン効率低下、ベルト/軸受不良、過剰冷却運転 |

### 8.6 改修フェーズでの活用（追加）
改修（制御改善・設定値見直し・最適化導入）では、改修前モデルをベースラインとして効果を定量化する。

- 年間（または期間）電力量の比較例
``` math
E_{base} = \sum_{t=1}^{N} P_{plant,base,t}\Delta t,\quad
E_{retrofit} = \sum_{t=1}^{N} P_{plant,retrofit,t}\Delta t
```

``` math
\Delta E = E_{base} - E_{retrofit},\quad
SavingRate = \frac{\Delta E}{E_{base}}\times 100
```

ここで $P_{plant}$ は少なくとも $P_{chiller}+P_{ct,fan}+P_{cw,pump}$ を含む指標として扱う（計測範囲に応じて定義を固定）。

### 8.7 Cxに必要な計測データ

| 分類 | 計測項目 | 変数 | 用途 |
|---|---|---|---|
| 制御対象 | 冷却水出口温度（冷却塔出口） | $T_{cw,out}$ | 追従性・残差算出 |
| 制御対象 | 冷却水入口温度（冷却塔入口） | $T_{cw,in}$ | 塔の温度差、異常検知補助 |
| 設定値 | 冷却水温度設定値 | $T_{cw,set}$ | 偏差算出、設定妥当性 |
| 操作量 | ファン速度（VFD） | $u_{fan}$ / `FAN_SPEED` | 出力一致、ハンチング検出 |
| 操作量 | ファン段数（段階制御） | `FAN_STAGE` | 状態遷移、チャタリング検出 |
| 操作量 | バイパス弁／三方弁開度 | `VALVE` | 過冷却防止、ロジック確認 |
| 状態 | ファン運転状態・台数 | `FAN_STATUS` / `CELL_ON` | ON/OFF一致、最小運転時間確認 |
| 外乱 | 外気湿球温度 | $T_{wb}$ | 外乱入力、妥当性（Approach）評価 |
| 流体条件 | 冷却水流量 | $m_{cw}$ / `FLOW_CW` | 能力・塔性能評価、異常検知 |
| 電力 | 冷却塔ファン電力 | $P_{fan}$ | 省エネ評価、機器劣化検知 |
| 電力 | 冷却水ポンプ電力（可能なら） | $P_{cw,pump}$ | システム最適評価 |
| 連携 | 冷凍機電力・運転台数 | $P_{chiller}$ / `CH_STATUS` | $P_{plant}$評価、外乱同定 |
| 補助 | 導電率・補給/ブロー（可能なら） | `COND` 等 | 水質制御の妥当性、運用影響整理 |

## 9.  単位

- 温度：℃
- 時間：s
- 導電率：mS/cm
- ファン速度：無次元（0～1）
- ファン段数：整数
- 弁開度：%
- 状態量：0/1 または整数
- 比例ゲイン Kp：1/℃
- 積分ゲイン Ki：1/(℃・s)

## 10.  実装上の注意

- 実機の条件分岐とモデルの条件分岐が一致していない場合、パラメータ調整のみでは再現できない。
- サンプリング周期 dt の不一致は PI 制御の応答差を生むため、必ず時間軸を整合させる。
- 温度ノイズが大きい場合は入力フィルタを設けるが、遅れの増加に注意する。
- 飽和制御を伴う PI 制御ではアンチワインドアップを適用する。
- 起動直後、停止直前、セル切替中は定常比較から除外する。
- BMSログでは入出力の単位と更新周期を必ず記録する。

## 11.  参考情報

- ASHRAE Guideline 36（高性能 HVAC 制御）
- ASHRAE Handbook – HVAC Systems and Equipment
- EnergyPlus Engineering Reference / EMS 関連仕様
- Modelica Buildings Library の制御ブロック
- BOPTEST documentation
- 国内 HVAC 制御実務資料、BMS/DDC 制御仕様書

