# 水-水熱交換器 計算仕様書（MATLAB/シムスペース ヒートエクスチェンジャー（TL-TL））

## 0.  文書基本情報

| 項目 | 内容 |
|---|---|
| 対象 | MathWorks Simscape Fluids `Heat Exchanger (TL-TL)` ブロック |
| 参照ページ | [https://jp.mathworks.com/help/hydro/ref/heatexchangertltl.html](https://jp.mathworks.com/help/hydro/ref/heatexchangertltl.html) |
| モデル名称 | 熱液体-熱液体熱交換器 |
| 目的 | 2つの単相熱液体流を、薄い伝導壁を介して熱交換させる。熱交換、圧力損失、流体容積および任意の壁熱容量をモデル化する。 |
| 計算状態 | 熱交換はε-NTU法を基礎とする。壁熱容量を有効にした場合は過渡応答を含む。相変化・潜熱は扱わない。 |

## 1.  基本情報

### 1.1 適用範囲・前提

- 各側の流体は単相のサーマルリキッド（Thermal Liquid）であり、顕熱だけを交換する。
- 熱物性は入口状態と流路内流体状態との平均値を熱計算に使用する。
- 流れ形式は並流・対向流、シェル&チューブ、直交流、又は有効度テーブル指定を選択できる。
- ウォール熱容量を無効にした場合は、熱交換を定常状態で扱う。ウォール熱容量を有効にすると、壁の蓄熱により温度・熱流束の応答に遅れが生じる。

### 1.2 処理の流れ

1. ポートA1/B1、A2/B2から両側の流体状態・流量を取得する。
2. 各側の熱容量流量、Reynolds数、Prandtl数、熱伝達係数、圧力損失を算定する。
3. 壁・汚れ・両側対流抵抗から総合熱抵抗、UA、NTUおよび有効度を求める。
4. 熱交換量・各側の出口状態を算定する。ウォール熱容量が有効なら壁のエネルギー収支を同時に解く。
5. ポート状態および物理信号出力Q1、Q2を更新する。

### 1.3 単位・記号

| 記号 | 単位 | 定義 |
|---|---|---|
| `m_dot_i` | kg/s | 液体iの質量流量 |
| `cp_i` | J/(kg K) | 液体iの定圧比熱 |
| `C_i = m_dot_i cp_i` | W/K | 液体iの熱容量流量 |
| `C_min`, `C_max` | W/K | 最小・最大熱容量流量 |
| `CR = C_min/C_max` | \- | 熱容量流量比 |
| `R` | K/W | 総合熱抵抗 |
| `UA = 1/R` | W/K | 総括伝熱性能 |
| `NTU = UA/C_min` | \- | 移動単位数 |
| `ε` | \- | 熱交換有効度 |
| `Q` | W | 熱移動量 |
| `Re`, `Pr`, `Nu` | \- | Reynolds数、Prandtl数、Nusselt数 |

## 2.  入出力インターフェイス

### 2.1 状態入力・出力

| ポート | 種別 | 定義 |
|---|---|---|
| A1, B1 | Conserving（Thermal Liquid 1） | 液体1の入口・出口。流向は計算状態により決まる。 |
| A2, B2 | Conserving（Thermal Liquid 2） | 液体2の入口・出口。流向は計算状態により決まる。 |
| Q1 | Physical Signal, W | 液体1への熱移動率。 |
| Q2 | Physical Signal, W | 液体2への熱移動率。 |

### 2.2 主なパラメータ

| 分類 | パラメータ | 単位 | 内容 |
|---|---|---|---|
| 共通 | Flow arrangement | \- | 並流・対向流、シェル&チューブ、直交流、又は有効度テーブル。 |
| 共通 | Wall thermal resistance | K/W | 隔壁の伝導熱抵抗。 |
| 共通 | Enable wall thermal mass | \- | ウォール熱容量の有効／無効。 |
| 共通 | Wall mass, Wall specific heat | kg, J/(kg K) | 壁の熱容量。 |
| 性能 | NTU vector, CR vector, E(NTU,CR) | \- | 汎用有効度テーブル。線形補間、最近傍外挿。 |
| 各側流路 | Minimum free-flow area, Hydraulic diameter | m2, m | 流速、Re、圧力損失の算定に用いる。 |
| 各側流路 | Thermal liquid volume | m3 | 流路内流体量。圧縮性・過渡計算に用いる。 |
| 圧力損失 | Pressure loss model | \- | 損失係数、管内相関、Darcy摩擦係数テーブル、Euler数テーブルから選択。 |
| 熱伝達 | Heat transfer coefficient model | \- | 定数h、管内相関、Colburn係数テーブル、Nu(Re,Pr)テーブルから選択。 |
| 熱伝達 | Area, fouling factor, minimum h | m2, m2 K/W, W/(m2 K) | 伝熱面積、汚れ抵抗、hの下限。 |

## 3.  計算ロジック

### 3.1 熱抵抗・UA

総合熱抵抗は、液体1側対流、液体1側汚れ、壁伝導、液体2側汚れ、液体2側対流の直列和である。

``` text
R = 1/(h_1 A_1) + F_1/A_1 + R_wall + F_2/A_2 + 1/(h_2 A_2)
```

`UA = 1/R`

`h_i` は定数、管内流相関、Colburn係数テーブル又は `Nu(Re,Pr)` テーブルから選択して求める。管内相関では、`Nu = h D_h/k` を用いる。汚れ係数 `F_i` は面積基準の熱抵抗である。

### 3.2 ε-NTU熱交換

``` text
C_i = m_dot_i cp_i
```

`C_min = min(C_1, C_2)`、`C_max = max(C_1, C_2)`、`CR = C_min / C_max`、`NTU = UA / C_min`

``` text
Q_max = C_min (T_hot,in - T_cold,in)
```

``` text
Q = ε(NTU,CR,流れ形式,混合条件,パス数) × Q_max
```

有効度式は流れ形式・混合条件・シェルパス数により選択される。直交流では混合条件を反映し、シェル&チューブではシェルパス数を反映する。汎用形式では、指定した `E(NTU,CR)` テーブルをNTU・CRで線形補間し、範囲外は最近傍値を用いる。

### 3.3 ウォール熱容量

ウォール熱容量が有効な場合、壁のエネルギー収支を解く。

``` text
M_wall cp_wall dT_wall/dt = Q_wall,1 + Q_wall,2
```

ここで `M_wall` は壁質量、`cp_wall` は壁比熱、`T_wall` は有効壁温度である。各流体への熱流は、流体入口温度・熱容量流量・流体-壁間NTUから求める。これによりヒートエクスチェンジャー壁の蓄熱に起因する過渡遅れを表現する。

### 3.4 圧力損失

各側の圧力損失は選択モデルにより算定する。

| モデル | 仕様書上の表現 |
|---|---|
| Pressure loss coefficient | `Δp = K ρ v² / 2`。Kは流路の局所・摩擦損失を合算した無次元損失係数。 |
| Correlation for flow inside tubes | Darcy摩擦係数、流路長、等価局所抵抗長、粗さ、層流形状係数から算定。層流では `f_D = K_lam / Re`。 |
| Darcy friction factor table | `f_D(Re)` を線形補間し、管摩擦式で算定。 |
| Euler number table | `Eu(Re)` を線形補間し、`Δp = Eu × ρ v² / 2` として算定。 |

管内相関の乱流域ではHaaland相関を基礎とし、層流上限Reと乱流下限Reの間は遷移域として補間する。

### 3.5 停止・成立条件

- 片側流量又は熱容量流量が実質的に0の場合、熱交換量は0へ収束する。
- 両流体は単相でなければならず、相変化は扱わない。
- 有効度テーブルを使う場合、NTUベクトル・CRベクトルは正かつ単調増加、テーブルは非負で整合したサイズでなければならない。
- ウォール熱容量が無効の場合、壁応答は瞬時と仮定する。

## 4.  精度検証

## 5.  パラメータ同定

## 6.  Cx・コミッショニングにおける使用場面

## 制約・適用上の留意事項

- 本仕様はMathWorks公式ヘルプページ（参照日：2026-08-04）に基づく。ブロック内部の個別実装コードは公開ページの記載範囲外である。
- 流体は単相の熱液体に限定され、沸騰・凝縮・凍結などの相変化は対象外である。
- コントロールバルブやポンプの制御ロジックは本ブロックの外部モデルで構成する。

