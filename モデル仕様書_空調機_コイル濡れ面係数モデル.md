# コイル濡れ面係数モデル

## 1.  基本情報

| 項目 | 内容 |
|---|---|
| モデルの名称 | コイル濡れ面係数モデル |
| モデルの概要 | 本モデルは、入口空気状態、入口水温、空気側風量および水側流量から、冷温水コイルの出口空気状態・出口水温・熱量・濡れ面係数を定常状態で算出するモデルである。風量比および水量比で熱通過能力（KA）を補正し、冷却時に出口空気の相対湿度が乾湿境界を超える場合は、顕熱比に応じた濡れ面係数を用いて除湿を含む熱交換を計算する。 |
| カテゴリ | 設備 - 空調 - 熱源・空調機器（冷温水コイル） |
| 参考文献 | 新晃工業技術資料、coil_wsf_model.m |

## 2.  入出力インターフェイス

### 2.1 入力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $T_{ai}$ | ℃DB | 数値(Real) | 入口空気乾球温度 |
| $X_{ai}$ | kg/kgDA | 数値(Real) | 入口空気絶対湿度 |
| $V$ | kg/s | 数値(Real) | 空気側質量流量 |
| $T_{wi}$ | ℃ | 数値(Real) | 入口水温 |
| $M$ | kg/s | 数値(Real) | 水側質量流量 |

### 2.2 出力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $T_{ao}$ | ℃DB | 数値(Real) | 出口空気乾球温度 |
| $X_{ao}$ | kg/kgDA | 数値(Real) | 出口空気絶対湿度 |
| $T_{wo}$ | ℃ | 数値(Real) | 出口水温 |
| $Q_t$ | kW | 数値(Real) | 空気側全熱量。冷却時は正の除去熱量、加熱時は負の値となる。 |
| $Q_s$ | kW | 数値(Real) | 空気側顕熱量 |
| $Q_l$ | kW | 数値(Real) | 空気側潜熱量 |
| $SHF$ | \- | 数値(Real) | 顕熱比 |
| $WSF$ | \- | 数値(Real) | 濡れ面係数。乾きコイル時は1。 |
| $KA$ | kW/K | 数値(Real) | 風量・水量補正後の熱通過能力 |

### 2.3 パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $V_d$ | kg/s | 数値(Real) | 定格空気側質量流量 |
| $M_{cd}$ | kg/s | 数値(Real) | 冷却時定格水側質量流量 |
| $M_{hd}$ | kg/s | 数値(Real) | 加熱時定格水側質量流量 |
| $KA_c$ | kW/K | 数値(Real) | 冷却時定格熱通過能力 |
| $KA_h$ | kW/K | 数値(Real) | 加熱時定格熱通過能力 |

### 2.4 内部定数

| 変数名 | 値 | 単位 | 定義 |
|:--:|----|----|----|
| $c_a$ | 1.006 | kJ/(kg·K) | 空気の比熱 |
| $c_w$ | 4.186 | kJ/(kg·K) | 水の比熱 |
| $e_l$ | 2501 | kJ/kg | 水の蒸発潜熱 |
| $RH_{bd}$ | 95 | % | 乾湿境界相対湿度 |
| $SHF_{min}$ | 0.38 | \- | 濡れ面係数の算定に用いる顕熱比の下限 |
| $p_{KA}$ | $[-0.18409, 0.74509, 0.92825, -0.010664, 3.2112, 0.0027578]$ | \- | 水量比・風量比によるKA補正係数 |
| $p_{WSF}$ | $[1.0972, -2.7088, 2.6121]$ | \- | 濡れ面係数の2次近似係数 |

## 3.  計算ロジック

### 3.1 演算条件

a\) 流量なし

以下のいずれかを満たす場合は熱交換を行わず、熱量・KA・顕熱比・濡れ面係数を0とする。出口空気状態および出口水温は入口状態を維持する。

``` math
M \leq 0 \quad \lor \quad V \leq 0
```

b\) 演算条件

``` math
M > 0 \quad \land \quad V > 0
```

の場合、3.2以降を実行する。

### 3.2 冷却・加熱モードの判定

入口空気乾球温度と入口水温の比較により、定格水量および定格KAを選択する。

``` math
\begin{cases}
T_{ai} > T_{wi}: & \text{冷却モード},\quad M_d=M_{cd},\quad KA_d=KA_c \\
T_{ai} \leq T_{wi}: & \text{加熱モード},\quad M_d=M_{hd},\quad KA_d=KA_h
\end{cases}
```

### 3.3 KA補正

水量比および風量比を以下で算定する。

``` math
r_M = \frac{M}{M_d}
```

``` math
r_V = \frac{V}{V_d}
```

各側の補正項を次式で算定する。

``` math
A_M = p_1 r_M^2 + p_2 r_M + p_3
```

``` math
A_V = p_4 r_V^2 + p_5 r_V + p_6
```

補正係数および補正後KAは次式とする。

``` math
r_{KA} = \frac{1}{\dfrac{1}{A_M}+\dfrac{1}{A_V}}
```

``` math
KA = KA_d\,r_{KA}
```

### 3.4 乾きコイルの出口状態

補正後KA、入口空気状態、入口水温および両側流量から、内蔵された対数平均温度差法により乾きコイルとしての出口空気乾球温度 $T_{ao,dry}$ と出口水温 $T_{wo,dry}$ を算定する。乾きコイルにおける水側熱量は次式である。

``` math
Q = (T_{wo,dry}-T_{wi})M c_w
```

乾きコイル出口温度 $T_{ao,dry}$ と入口絶対湿度 $X_{ai}$ から、内蔵された湿り空気状態計算により出口相対湿度 $RH_{ao,dry}$ を算定する。

### 3.5 乾湿判定

以下の条件をともに満たす場合は濡れコイルとして3.6を実行する。それ以外は乾きコイルとして3.7を実行する。

``` math
RH_{ao,dry} > RH_{bd}
```

``` math
\text{冷却モード}
```

### 3.6 濡れコイル計算

a\) 探索範囲

出口空気乾球温度の探索下限を乾きコイルの出口温度、上限を入口絶対湿度 $X_{ai}$ において相対湿度が95 %となる乾球温度とする。

``` math
T_1 = T_{ao,dry}
```

``` math
T_2 = T_{db}(RH_{bd},X_{ai})
```

b\) 候補温度における空気側状態

候補出口温度 $T$ に対して、内蔵された湿り空気状態計算により、相対湿度95 %での出口絶対湿度 $X_{ao}$ を求める。

``` math
X_{ao} = X(T,RH_{bd})
```

空気側の顕熱量、潜熱量、全熱量および顕熱比を求める。

``` math
Q_s = (T_{ai}-T)V c_a
```

``` math
Q_l = (X_{ai}-X_{ao})V e_l
```

``` math
Q_t = Q_s + Q_l
```

``` math
SHF = \frac{Q_s}{Q_t}
```

c\) 濡れ面係数と熱交換器能力

濡れ面係数は、顕熱比に下限を適用した2次式で求める。

``` math
SHF' = \max(SHF,SHF_{min})
```

``` math
WSF = 1.0972\,{SHF'}^2 - 2.7088\,SHF' + 2.6121
```

候補温度に対応する出口水温、対数平均温度差および熱交換器能力を求める。

``` math
T_{wo} = T_{wi} + \frac{Q_t}{M c_w}
```

``` math
\Delta T_{lm} = \frac{(T_{ai}-T_{wo})-(T-T_{wi})}
{\ln\left(\dfrac{T_{ai}-T_{wo}}{T-T_{wi}}\right)}
```

``` math
Q_{HEX} = KA\,\Delta T_{lm}\,WSF
```

d\) 二分法による出口温度の決定

以下の残差の零点を探索する。

``` math
f(T) = Q_{HEX} - Q_t
```

初期区間の両端で残差の符号が異なる場合は、20回を上限として二分法を実行する。中点の残差の符号に基づき、根を含む半区間を次の探索区間とする。残差の絶対値が0.1 kW未満になった時点で収束とする。

``` math
|f(T)| < 0.1
```

初期区間の両端で残差の符号が同じ場合は、残差の絶対値が小さい端点を採用する。探索終了後は、採用した出口温度で空気側状態および水側状態を再計算して出力値を確定する。

### 3.7 乾きコイル時の出力

乾湿判定で濡れコイルとならない場合、絶対湿度は変化しないものとする。

``` math
X_{ao} = X_{ai}
```

``` math
Q_t = Q_s = Q
```

``` math
Q_l = 0
```

``` math
SHF = 1
```

``` math
WSF = 1
```

## 4.  精度検証

### 4.1 検証方法

シンガポールの鹿島建設施設内で計測した2日間の冷水コイル周りのデータを用いて検証を行なった。10分刻みのデータの各時刻についてモデル計算を行い、出口空気温度、出口水温および交換熱量を比較した。空調の立ち上がり過渡を除外するため、各運転区間の開始から30分未満は検証対象外とした。

### 4.2 検証条件

| 項目 | 設定値 |
|----|----:|
| 実測CSV総行数 | 289件 |
| 検証対象行数 | 95件 |
| 立ち上がり除外時間 | 30分 |
| 定格水量 | 40.8 L/min |
| 定格風量 | 6,560 m<sup>3</sup>/h |
| 設計 $KA_c$ | 3.367 kW/K |

### 4.3 検証結果

設計値の $KA_c$ を用いた比較結果を以下に示す。熱量は実測値より大きく算定される傾向がある。

| 比較項目 | MAE | RMSE | CVRMSE |
|----|----:|----:|----:|
| 出口空気温度 | 2.93 ℃ | 3.13 ℃ | \- |
| 出口水温 | 1.85 ℃ | 1.92 ℃ | \- |
| 交換熱量 | 4.29 kW | 4.79 kW | 28.07 % |

![設計KAによる実測値と計算値の比較](coil_wsf_model_validation_plot.png)

## 5.  パラメータ同定

定格風量 $V_d$、定格水量 $M_{cd}$は設計仕様で固定とし、$KA_c$を同定する。

### 5.1 $KA_c$ の同定方法

4章と同じ定常実測データ95件に対して交換熱量のRMSEを目的関数とし、$KA_c$ を探索した。

``` math
RMSE_Q = \sqrt{\frac{1}{N}\sum_{i=1}^{N}\left(\left|Q_{model,i}\right|-Q_{measured,i}\right)^2}
```

探索範囲は設計 $KA_c$ の20 %から300 %、探索手法は黄金分割探索、反復回数は40回とした。

### 5.2 同定結果

| 項目 | 設計値 | 同定値 |
|----|----:|----:|
| $KA_c$ | 3.367 kW/K | 2.246 kW/K |
| 設計値に対する比 | 1.00 | 0.667 |
| 交換熱量RMSE | 4.79 kW | 0.44 kW |
| 出口空気温度RMSE | 3.13 ℃ | 1.52 ℃ |
| 出口水温RMSE | 1.92 ℃ | 0.29 ℃ |

熱量RMSEを最小化する目的で同定した結果、$KA_c$ は設計値の約66.7 %となった。全熱ベースではよく一致しているものの、特に1日目の低負荷運転時に出口空気温度が低くなっており、顕熱・潜熱の内訳の誤差が大きくなる傾向がある。この点を解消するためには $p_{KA}$ や $p_{WSF}$ の同定を検討する必要がある。

![設計KAと同定KAによる実測値との比較](coil_wsf_model_kac_comparison_plot.png)

## 6.  Cxにおける使用場面の想定

