# コイル濡れ面係数モデル

## 1. 基本情報

| 項目 | 内容 |
|---|---|
| 名称 | 冷温水コイルモデル（濡れ面係数） |
| 概要 | 入口空気温湿度、入口水温、風量および流量から、冷温水コイルの出口空気温湿度、出口水温および交換熱量を定常状態で算出する。冷却時に乾きコイル出口の相対湿度が乾湿境界を超える場合は、濡れ面係数を用いて除湿を含む熱交換を計算する。 |
| カテゴリ | 設備 - 空調 - 空調機 - 冷温水コイル |
| 参考文献 | 新晃工業技術資料、`coil_wsf_model.py` |

## 2. 入出力インターフェイス

### 2.1 入力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $T_{ai}$ | ℃DB | 数値（Real） | 入口空気乾球温度。 |
| $X_{ai}$ | kg/kgDA | 数値（Real） | 入口空気絶対湿度。 |
| $V$ | kgDA/s | 数値（Real） | 空気側質量流量。 |
| $T_{wi}$ | ℃ | 数値（Real） | 入口水温。 |
| $M$ | kg/s | 数値（Real） | 水側質量流量。 |

### 2.2 出力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $T_{ao}$ | ℃DB | 数値（Real） | 出口空気乾球温度。 |
| $X_{ao}$ | kg/kgDA | 数値（Real） | 出口空気絶対湿度。 |
| $T_{wo}$ | ℃ | 数値（Real） | 出口水温。 |
| $Q_t$ | kW | 数値（Real） | 空気側の処理熱量。冷却時の除去熱量を正、加熱時を負とする。 |
| $Q_s$ | kW | 数値（Real） | 空気側の顕熱処理熱量。冷却時を正、加熱時を負とする。 |
| $Q_l$ | kW | 数値（Real） | 空気側の潜熱処理熱量。 |
| $SHF$ | \- | 数値（Real） | 顕熱比。濡れ面係数の算定および結果検証に用いる。 |
| $WSF$ | \- | 数値（Real） | 濡れ面係数。乾きコイル時は1、無流量時は0。 |
| $KA$ | kW/K | 数値（Real） | 風量・水量補正後の熱通過能力。結果検証に用いる。 |

### 2.3 パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $V_d$ | kgDA/s | 数値（Real） | 定格空気側質量流量。$V_d>0$。 |
| $M_{cd}$ | kg/s | 数値（Real） | 定格冷却水側質量流量。$M_{cd}>0$。 |
| $M_{hd}$ | kg/s | 数値（Real） | 定格加熱水側質量流量。$M_{hd}>0$。 |
| $KA_{cd}$ | kW/K | 数値（Real） | 定格冷却時熱通過能力。$KA_{cd}>0$。 |
| $KA_{hd}$ | kW/K | 数値（Real） | 定格加熱時熱通過能力。$KA_{hd}>0$。 |

### 2.4 内部定数

| 変数名 | 値 | 単位 | 定義 |
|:--:|----|----|----|
| $c_a$ | 1.006 | kJ/(kgDA·K) | 空気の比熱。 |
| $c_w$ | 4.186 | kJ/(kg·K) | 水の比熱。 |
| $e_l$ | 2501 | kJ/kg | 水の蒸発潜熱。 |
| $P$ | 101325 | Pa | 湿り空気計算の基準大気圧。 |
| $RH_{bd}$ | 0.95 | \- | 乾湿境界相対湿度。比率（0～1）で表す。 |
| $SHF_{min}$ | 0.38 | \- | 濡れ面係数算定用の顕熱比下限。 |
| $\varepsilon_Q$ | $1.0\times10^{-12}$ | kW | $Q_t$が零とみなせる閾値。 |
| $\varepsilon_f$ | 0.1 | kW | 濡れコイル熱収支残差の収束判定値。 |
| $N_{bis}$ | 20 | \- | 濡れコイル二分法の最大反復回数。 |
| $p_{KA}$ | $[p_1,\ldots,p_6]$ | \- | $[-0.18409,0.74509,0.92825,-0.010664,3.2112,0.0027578]$。KA補正係数。 |
| $p_{WSF}$ | $[a,b,c]$ | \- | $[1.0972,-2.7088,2.6121]$。濡れ面係数の2次近似係数。 |

## 3. 計算ロジック

### 3.1 入力検証および無流量時

全ての入力・パラメータは有限値とする。$X_{ai}\geq0$、$V\geq0$、$M\geq0$、および2.3節の正値制約を満たさない場合は入力エラーとして計算を中止する。$V=0$または$M=0$は無流量として次式を出力する。

``` math
T_{ao}=T_{ai},\quad X_{ao}=X_{ai},\quad T_{wo}=T_{wi}
```

``` math
Q_t=Q_s=Q_l=SHF=WSF=KA=0
```

### 3.2 冷却・加熱モードおよびKA補正

``` math
\begin{cases}
T_{ai}>T_{wi}:&\text{冷却},\quad M_d=M_{cd},\quad KA_d=KA_{cd}\\
T_{ai}\leq T_{wi}:&\text{加熱},\quad M_d=M_{hd},\quad KA_d=KA_{hd}
\end{cases}
```

$T_{ai}=T_{wi}$では加熱側パラメータを選択するが、3.3節より熱交換量は0となる。流量比と補正項を以下で求める。

``` math
r_M=\frac{M}{M_d},\quad r_V=\frac{V}{V_d}
```

``` math
A_M=p_1r_M^2+p_2r_M+p_3,\quad A_V=p_4r_V^2+p_5r_V+p_6
```

``` math
KA=\frac{KA_d}{1/A_M+1/A_V}
```

$A_M\leq0$、$A_V\leq0$または$KA\leq0$となる流量比は有効範囲外とし、入力エラーとして計算を中止する。

### 3.3 乾きコイルの出口状態

乾きコイルは対向流熱交換器のNTU–有効度法で解く。熱容量流量、容量比およびNTUを次式で定義する。

``` math
C_a=Vc_a,\quad C_w=Mc_w,\quad C_{min}=\min(C_a,C_w),\quad C_{max}=\max(C_a,C_w)
```

``` math
R_C=\frac{C_{min}}{C_{max}},\quad NTU=\frac{KA}{C_{min}}
```

対向流有効度$\eta$は、$|1-R_C|\leq10^{-12}$の場合とそれ以外で次式とする。

``` math
\eta=\frac{NTU}{1+NTU}\quad (R_C=1)
```

``` math
\eta=\frac{1-\exp\{-NTU(1-R_C)\}}{1-R_C\exp\{-NTU(1-R_C)\}}\quad (R_C\ne1)
```

``` math
Q_{dry}=\eta C_{min}(T_{ai}-T_{wi})
```

``` math
T_{ao,dry}=T_{ai}-\frac{Q_{dry}}{C_a},\quad T_{wo,dry}=T_{wi}+\frac{Q_{dry}}{C_w}
```

これらは次の連立熱収支$Q_{dry}=C_a(T_{ai}-T_{ao,dry})=C_w(T_{wo,dry}-T_{wi})$を、NTU–有効度法で陽に解いた式である。

### 3.4 湿り空気状態計算

温度$T$を℃、絶対温度を$\theta=T+273.15$ [K]とする。飽和蒸気圧$P_{ws}$[Pa]は$P_{ws}=f_{Pws}(T)$で求める。$T\leq0.01$ ℃では次式、$T>0.01$ ℃では後式を用いる。

``` math
\ln P_{ws}=-\frac{5674.5359}{\theta}+6.3925247-0.009677843\theta+0.00000062215701\theta^2+0.0000000020747825\theta^3-0.0000000000009484024\theta^4+4.1635019\ln\theta
```

``` math
\ln P_{ws}=-\frac{5800.2206}{\theta}+1.3914993-0.048640239\theta+0.000041764768\theta^2-0.000000014452093\theta^3+6.5459673\ln\theta
```

絶対湿度$X$、水蒸気分圧$P_w$および相対湿度$RH$の換算は次式とする。

``` math
P_w=\frac{PX}{0.621945+X},\quad RH=f_{RH}(T,X)=\frac{P_w}{P_{ws}}
```

``` math
X=f_X(T,RH)=\frac{0.621945\,RH\,f_{Pws}(T)}{P-RH\,f_{Pws}(T)}
```

$T_{db}=f_{Tdb}(RH,X)$は、$f_X(T,RH)-X=0$を二分法で解く逆算乾球温度とする。$0<RH\leq1$および$X\geq0$を入力条件とし、初期区間を$[T_L,T_U]=[-20,50]$ ℃とする。

``` math
T_M=\frac{T_L+T_U}{2}
```

``` math
\begin{cases}
f_X(T_M,RH)<X:&T_L\leftarrow T_M\\
f_X(T_M,RH)\geq X:&T_U\leftarrow T_M
\end{cases}
```

60回終了後、$T_{db}=(T_L+T_U)/2$を採用する。

### 3.5 乾湿判定

乾きコイル出口相対湿度を$RH_{ao,dry}=f_{RH}(T_{ao,dry},X_{ai})$とする。冷却かつ$RH_{ao,dry}>RH_{bd}$の場合を濡れコイルとし、3.6節を実行する。それ以外は乾きコイルとして次式を出力する。

``` math
T_{ao}=T_{ao,dry},\quad X_{ao}=X_{ai},\quad T_{wo}=T_{wo,dry}
```

``` math
Q_t=Q_s=Q_{dry},\quad Q_l=0,\quad SHF=WSF=1
```

### 3.6 濡れコイルの出口状態

探索区間は次式とする。

``` math
T_1=T_{ao,dry},\quad T_2=f_{Tdb}(RH_{bd},X_{ai})
```

乾湿判定が成立する有効な数値計算では$T_1\leq T_2$となる。数値誤差等で$T_1>T_2$の場合、または有効なLMTD候補が存在しない場合は濡れ計算を行わず、3.5節の乾きコイル出力を採用する。

候補温度$T$に対し、次式で状態と熱量を算出する。

``` math
X_{ao}=f_X(T,RH_{bd}),\quad Q_s=(T_{ai}-T)Vc_a,\quad Q_l=(X_{ai}-X_{ao})Ve_l
```

``` math
Q_t=Q_s+Q_l
```

$|Q_t|\leq\varepsilon_Q$の場合は$SHF=1$とする。それ以外は$SHF=Q_s/Q_t$とする。

``` math
SHF'=\max(SHF,SHF_{min}),\quad WSF=f_{WSF}(SHF')=a{SHF'}^2+bSHF'+c
```

``` math
T_{wo}=T_{wi}+\frac{Q_t}{Mc_w}
```

濡れコイルのLMTDは$\Delta T_1=T_{ai}-T_{wo}$、$\Delta T_2=T-T_{wi}$から求める。両者が正で、かつ差が$10^{-12}$ K以下の場合は算術平均を用いる。

``` math
\Delta T_{lm}=f_{LMTD}(\Delta T_1,\Delta T_2)=\frac{\Delta T_1+\Delta T_2}{2}\quad (|\Delta T_1-\Delta T_2|\leq10^{-12})
```

``` math
\Delta T_{lm}=f_{LMTD}(\Delta T_1,\Delta T_2)=\frac{\Delta T_1-\Delta T_2}{\ln(\Delta T_1/\Delta T_2)}\quad (\Delta T_1>0\land\Delta T_2>0)
```

$\Delta T_1\leq0$または$\Delta T_2\leq0$では、終端温度差ゼロ、符号反転または温度交差によりLMTDは定義しない。この候補を無効とする。正の等温度差は上式の算術平均で扱う。したがって、ゼロ除算および対数の符号反転は行わない。

``` math
Q_{HEX}=KA\,\Delta T_{lm}\,WSF,\quad f_{res}(T)=Q_{HEX}-Q_t
```

端点$T_1,T_2$で有効な候補を評価する。$|f_{res}(T_1)|\leq\varepsilon_f$または$|f_{res}(T_2)|\leq\varepsilon_f$なら、その端点を採用する。両端の残差が異符号なら、中点$T=(T_1+T_2)/2$を初期候補とする二分法を最大$N_{bis}$回実行する。残差が許容値内なら直ちに採用し、そうでなければ符号が変わる半区間を次の区間とする。初期端点が同符号の場合、および$N_{bis}$回で未収束の場合は、最後に評価した端点と中点のうち$|f_{res}(T)|$が最小の候補を採用する。採用温度で本節の状態式を再計算して最終出力とする。

## 4. 代表計算例

無流量の例として、$T_{ai}=27$ ℃、$X_{ai}=0.010$ kg/kgDA、$V=0$ kgDA/s、$T_{wi}=7$ ℃、$M=0.68$ kg/sを入力する。この場合、3.1節より次式が得られる。

``` math
T_{ao}=27\ \mathrm{℃},\quad X_{ao}=0.010\ \mathrm{kg/kgDA},\quad T_{wo}=7\ \mathrm{℃}
```

``` math
Q_t=Q_s=Q_l=SHF=WSF=KA=0
```

また、$T_{ai}=T_{wi}$かつ両流量が正のとき、3.3節より$Q_{dry}=0$であり、乾きコイル経路で入口・出口温度および絶対湿度は同一となる。

## 5. 精度検証

シンガポールの鹿島建設施設内で計測した2日間の冷水コイル周りのデータを用いて検証を行った。10分刻みの各時刻を計算し、運転区間開始から30分未満を除外した。

| 項目 | 設定値 |
|---|---:|
| 実測CSV総行数 | 289件 |
| 検証対象行数 | 95件 |
| 立ち上がり除外時間 | 30分 |
| 定格水量 | 40.8 L/min |
| 定格風量 | 6,560 m<sup>3</sup>/h |
| 設計 $KA_c$ | 3.367 kW/K |

| 比較項目 | MAE | RMSE | CVRMSE |
|---|---:|---:|---:|
| 出口空気温度 | 2.93 ℃ | 3.13 ℃ | \- |
| 出口水温 | 1.85 ℃ | 1.92 ℃ | \- |
| 交換熱量 | 4.29 kW | 4.79 kW | 28.07 % |

![設計KAによる実測値と計算値の比較](png/coil_wsf_model_validation_plot.png)

## 6. パラメータ同定

定格風量$V_d$、定格水量$M_{cd}$は設計仕様で固定とし、$KA_{cd}$を同定する。95件の定常実測データに対する熱量RMSEを目的関数として、設計$KA_{cd}$の20 %から300 %の範囲を黄金分割探索（40反復）する。

``` math
RMSE_Q=\sqrt{\frac{1}{N}\sum_{i=1}^{N}\left(\left|Q_{model,i}\right|-Q_{measured,i}\right)^2}
```

| 項目 | 設計値 | 同定値 |
|---|---:|---:|
| $KA_{cd}$ | 3.367 kW/K | 2.246 kW/K |
| 設計値に対する比 | 1.00 | 0.667 |
| 交換熱量RMSE | 4.79 kW | 0.44 kW |
| 出口空気温度RMSE | 3.13 ℃ | 1.52 ℃ |
| 出口水温RMSE | 1.92 ℃ | 0.29 ℃ |

![設計KAと同定KAによる実測値との比較](png/coil_wsf_model_kac_comparison_plot.png)

## 7. Cxにおける使用場面の想定
