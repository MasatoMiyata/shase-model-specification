# 水-水熱交換器 計算仕様書（対向流制御モデル）

## 0.  文書基本情報

| 項目 | 内容 |
|---|---|
| 参照資料 | 対向流型水対水熱交換器モデルの原典マニュアル、3章「対向流型水対水熱交換器モデル(HEX)」 |
| モデル名称 | 対向流型水対水熱交換器（HEX） |
| 目的 | プレート型等の対向流熱交換器について、熱交換量、出口温度、流量制御および圧力損失を算定する。 |
| 計算方式 | 対数平均温度差（LMTD）法による定常物理モデル。 |
| モデル種別 | タイプ1（性能計算）、タイプ2（一次側流量制御）、タイプ2B（一次側三方弁分流）、タイプ3（二次側三方弁分流）。 |

## 1.  基本情報

### 1.1 適用範囲・前提

- 対向流型の水-水熱交換器を対象とする。プレート熱交換器を主な適用例とする。
- 熱通過率は流速依存性を持つが、基準流量から流量比の0.8乗で表面伝達係数が変化するとして補正する。
- 金属パイプ又はプレートの熱抵抗は無視する。
- 定常状態を対象とし、熱交換器壁の蓄熱、配管熱損失および動的制御遅れは扱わない。

### 1.2 モデル種別

| 種別 | 概要 | 操作量 |
|---|---|---|
| タイプ1 | 入口温度・流量から、LMTD法で出口温度と熱量を求める基本モデル。 | なし |
| タイプ2 | 二次側出口温度を一定にするため、一次側流量を直接制御する。インバータポンプを想定。 | 一次側流量 |
| タイプ2B | タイプ2の変形。一次側流量を与え、三方弁による一次側分流量を計算して二次側出口温度を制御する。 | 一次側三方弁配分 |
| タイプ3 | 二次側三方弁の流量配分により二次側出口温度を制御する。 | 二次側三方弁配分 |

### 1.3 単位・記号

| 記号 | 単位 | 定義 |
|---|---|---|
| `T1i`, `T1o` | ℃ | 一次側入口・出口温度 |
| `T2i`, `T2o` | ℃ | 二次側入口・出口温度 |
| `M1`, `M2` | kg/s | 一次側・二次側流量 |
| `Mr1`, `Mr2` | kg/s | 一次側・二次側定格流量 |
| `Cp1`, `Cp2` | kJ/(kg K) | 一次側・二次側比熱 |
| `A` | m2 | 伝熱面積 |
| `Uc`, `Ud` | kW/(m2 K) | 清浄時・流量補正後の熱通過率 |
| `Q_HEX` | kW | 熱交換器交換熱量 |
| `dP1`, `dP2` | kPa | 一次側・二次側圧力損失 |

## 2.  入出力インターフェイス

### 2.1 タイプ1の入力・出力

| 分類 | 変数 | 単位 | 定義 |
|---|---|---|---|
| 入力 | `konoff` | \- | 運転指標。冷却=1、加熱=-1、停止=0。 |
| 入力 | `T1i`, `M1` | ℃, kg/s | 一次側入口温度・流量。 |
| 入力 | `T2i`, `M2` | ℃, kg/s | 二次側入口温度・流量。 |
| 出力 | `T1o`, `T2o` | ℃ | 一次側・二次側出口温度。 |
| 出力 | `Q_HEX` | kW | 交換熱量。 |
| 出力 | `dP1`, `dP2` | kPa | 各側圧力損失。 |
| 出力 | HEX検証出力 | \- | 検証BOX接続用ベクトル。 |

### 2.2 タイプ2・タイプ2B・タイプ3の入出力

| 種別 | 追加入力 | 主要出力 |
|---|---|---|
| タイプ2 | `M1s`（一次側最大可能流量）、`T2o,set` | `M1`（制御後一次側流量）、`T1o`、`T2o`、`Q_HEX`、`dP1`、`dP2` |
| タイプ2B | `M1s`、`T2o,set` | 三方弁分流後の`M1`、`T1o`、`T2o`、`Q_HEX`、`dP1`、`dP2` |
| タイプ3 | `M1`、`M2`、`T2o,set` | `T1o`、`T2o`、`Q_HEX`、`dP1`、`dP2` |

### 2.3 パラメータ

| パラメータ | 単位 | 定義 |
|---|---|---|
| `HEXname` | \- | 熱交換器名称。MFモデルでは任意数値。 |
| `Uc` | kW/(m2 K) | 清浄時の熱通過率。 |
| `Mr1`, `Mr2` | kg/s | 定格流量。 |
| `A` | m2 | 伝熱面積。 |
| `Cp1`, `Cp2` | kJ/(kg K) | 比熱。例：水4.186、不凍液ブライン3.708。 |
| `dPrk1`, `dPrk2` | kPa | 各側の定格圧力損失。 |

メーカー性能表の流量、伝熱面積、比熱、圧力損失を単位換算して設定する。`Uc`は性能表にないことが多く、定格熱量とLMTDから逆算する。

## 3.  計算ロジック

### 3.1 流量補正後熱通過率

流量が定格から変化したとき、各側の表面伝達係数は流量比の0.8乗に比例するとして、流量補正後熱通過率を次式で算定する。

$$Ud = 1 / [{1/(2 Uc (M1/Mr1)^0.8)} + {1/(2 Uc (M2/Mr2)^0.8)}]$$

これは清浄時熱通過率 `Uc` を両側の対流抵抗へ等分し、各側流量で補正した直列熱抵抗式である。

### 3.2 LMTDと熱量

温度差を次式で定義する。

$$ΔTa = T2i - T1o$$

$$ΔTb = T2o - T1i$$

$$Δtm = (ΔTa-ΔTb) / ln(ΔTa/ΔTb)$$

ΔTa=ΔTb の場合は、Δtm=ΔTa とする。熱交換量は以下である。

$$Q_HEX = Ud × A × Δtm$$

熱収支は以下を満たす。

$$Q_HEX = Cp1 M1 (T1o-T1i) = Cp2 M2 (T2o-T2i)$$

注：マニュアルの式表記には添字の不整合が見られるため、本仕様では「各側の熱容量流量×その側の入口・出口温度差」という熱収支として解釈する。符号は実装の冷却／加熱モードに従う。

### 3.3 圧力損失

各側の圧力損失は、定格圧力損失と実流量の関係から算定する。マニュアル該当頁では詳細式は明示されていないため、`dPrk1`、`dPrk2` を基準とするモデル内部の圧損計算として扱う。性能検証では流量-差圧曲線をメーカー値と照合する。

### 3.4 タイプ2・タイプ2B・タイプ3の制御

- **タイプ2**：二次側出口設定温度 `T2o,set` を満たすよう、一次側流量を `0 ≤ M1 ≤ M1s` の範囲で求める。ポンプの最小流量がある場合は、別途バイパスを設ける。
- **タイプ2B**：一次側最大可能流量を与え、三方弁の配分を計算して本体通過流量を決める。弁の動特性はモデル化せず、計算上で配分量を直接求める。
- **タイプ3**：一次・二次側の入口温度・流量を与え、二次側三方弁の分流により `T2o,set` を制御する。熱収支から一次側出口温度を求める。

### 3.5 停止・能力制約

- `konoff=0` は停止であり、熱交換を行わない。
- 冷却時に被冷却流体が熱源流体より低温の場合、又は加熱時に被加熱流体が熱源流体より高温の場合は、熱交換できないため入口温度を出力する。
- タイプ2、タイプ2B、タイプ3で設定温度が熱交換器能力を超える場合は、設定温度を達成せず、最大能力時の出口温度を出力する。

## 4.  精度検証

## 5.  パラメータ同定

## 6.  Cx・コミッショニングにおける使用場面

## 制約・適用上の留意事項

- 本仕様は原典マニュアルの公開記載に基づく。モデル内部のブロック実装・反復アルゴリズムの詳細は資料に記載されない。
- 金属壁抵抗を無視する近似であり、厚肉壁、低伝導材又は著しい汚れを持つ熱交換器では追加検討が必要である。
- 動特性、弁のストローク特性、ポンプ曲線、圧力損失の詳細相関は本資料のモデル範囲外である。

