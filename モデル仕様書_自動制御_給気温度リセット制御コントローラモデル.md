# VAV給気温度リセット制御コントローラモデル

## 1.  基本情報

| 項目 | 内容 |
|---|---|
| モデルの名称 | VAV給気温度リセット制御コントローラモデル |
| モデルの概要 | 本モデルは、複数VAVの制御ステータスを一定周期で集計し、空調機の給気温度設定値をリセットする離散時間コントローラである。VAVの能力増要求、適正化要求、最小風量状態を総合して給気温度の増減幅を決定する。 |
| カテゴリ | 設備 - 空調 - 空調機制御（VAV給気温度リセット） |
| 参考文献 | 170616_次世代超高層_DAIKINより受領_給気温度制御.pdf、loadreset_controller_v4.m |

## 2.  入出力インターフェイス

### 2.1 入力項目

| 配列 | 変数名 | 単位 | データ型 | 定義 |
|:--:|:--:|----|----|----|
| `u(1)` | $Sw$ | \- | 状態フラグ | 空調機運転スイッチ。正の値で運転と判定する。 |
| `u(2)` | $T_o$ | ℃ | 数値(Real) | 外気温度。現行実装では読み込むが計算には使用しない。 |
| `u(3)` | $T_r$ | ℃ | 数値(Real) | 室温。現行実装では読み込むが計算には使用しない。 |
| `u(4)` | $preSw$ | \- | 状態フラグ | 前時刻の空調機運転スイッチ |
| `u(5)` | $preT_{sp}$ | ℃ | 数値(Real) | 前時刻の給気温度設定値 |
| `u(6)` | $precount$ | s | 数値(Real) | 前時刻の給気温度リセット周期カウンタ |
| `u(9)`～`u(8+Nvav)` | $sts_i$ | \- | 整数 | 各VAVの制御ステータス |
| `u(18)` | $CH$ | \- | 整数 | 現時刻の冷暖房モード（0:送風、1:冷房、2:暖房） |
| `u(19)` | $preCH$ | \- | 整数 | 前時刻の冷暖房モード。現行実装では無効化された分岐内のみで参照する。 |
| `u(20)` | $pretsts$ | \- | 整数 | 前時刻の総合制御ステータス |

注記：$CH$、$preCH$、$pretsts$ の参照位置は固定である。このため、VAVステータスを `u(9)`～`u(17)` に格納する $Nvav=9$ の構成で使用することを前提とする。$Nvav$ を変更する場合は、後続入力の添字も合わせて変更する必要がある。

### 2.2 出力項目

| 配列 | 変数名 | 単位 | データ型 | 定義 |
|:--:|:--:|----|----|----|
| `y(1)` | $T_{sp}$ | ℃ | 数値(Real) | 給気温度設定値 |
| `y(2)` | $dT$ | K | 数値(Real) | 今回の給気温度設定値リセット幅 |
| `y(3)` | $tsts$ | \- | 整数 | VAVステータスから集計した総合制御ステータス |
| `y(4)` | $count$ | s | 数値(Real) | 給気温度リセット周期カウンタ |

### 2.3 パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $dt$ | s | 数値(Real) | モデルの計算周期 |
| $Nvav$ | \- | 整数 | 集計対象VAVの台数。現行の固定添字では9を想定する。 |
| $t_{clr}$ | s | 数値(Real) | 給気温度リセット制御周期 |
| $T_{smin}$ | ℃ | 数値(Real) | 給気温度設定値の下限 |
| $T_{smax}$ | ℃ | 数値(Real) | 給気温度設定値の上限 |
| $T_{sp,ini}$ | ℃ | 数値(Real) | 運転開始時の給気温度設定値 |

### 2.4 内部定数

| 変数名 | 値 | 単位 | 定義 |
|:--:|----:|----|----|
| $dT_{i1}$ | 1.0 | K | 暖房能力不足時の大きい上昇幅 |
| $dT_{i2}$ | 0.5 | K | 小さい上昇幅 |
| $dT_{d1}$ | -1.0 | K | 冷房能力不足時の大きい低下幅 |
| $dT_{d2}$ | -0.5 | K | 小さい低下幅 |
| $T_{stc}$ | 14 | ℃ | 冷房時標準給気温度。現行実装では使用しない。 |
| $T_{sth}$ | 30 | ℃ | 暖房時標準給気温度。現行実装では使用しない。 |

## 3.  計算ロジック

### 3.1 運転・停止時の処理

空調機が停止の場合、給気温度設定値および総合制御ステータスを前時刻値に保持する。カウンタおよびリセット幅は0とする。

``` math
Sw \leq 0
```

``` math
count=0,\quad T_{sp}=preT_{sp},\quad tsts=pretsts,\quad dT=0
```

空調機が運転の場合、まずカウンタを更新する。

``` math
Sw>0
```

``` math
count=precount+dt
```

運転開始時（$preSw=0$）は、以下のとおり初期化する。カウンタは上式で更新した値を維持する。

``` math
T_{sp}=T_{sp,ini},\quad tsts=pretsts,\quad dT=0
```

### 3.2 給気温度リセット周期

継続運転時、カウンタが制御周期に達した場合にのみVAVステータスを集計してリセット幅を更新する。

``` math
count\geq t_{clr}
```

この場合、カウンタを0にリセットする。制御周期に達していない場合は、総合制御ステータスを前時刻値に保持し、リセット幅を0とする。

### 3.3 VAVステータスの集計

各VAVのステータスをコード別に計数する。ステータス0は停止としてコード8へ計上する。

| コード | VAVステータス |
|:--:|----|
| 1 | 冷房能力増要求 |
| 2 | 適正冷房 |
| 3 | 最小風量（能力過剰） |
| 4 | 暖房適正化要求 |
| 5 | 暖房能力増要求 |
| 6 | 冷房適正化要求 |
| 7 | 適正暖房 |
| 8 | 停止VAVの集計値、または総合状態が不定 |

各コードの台数を $Nsts(j)$ とする。総合制御ステータス $tsts$ は、次の優先順で決定する。

| 優先 | 条件 | $tsts$ |
|:--:|----|:--:|
| 1 | 全VAVが停止 | 8 |
| 2 | コード1または5が存在し、$Nsts(1)>Nsts(5)$ | 1 |
| 2 | コード1または5が存在し、$Nsts(1)<Nsts(5)$ | 5 |
| 2 | コード1と5が同数 | 8 |
| 3 | 暖房側コード4・7がなく、コード3が存在 | 3 |
| 3 | 暖房側コード4・7がなく、コード3がなく、コード2が存在 | 2 |
| 3 | 暖房側コード4・7がなく、コード3がなく、コード2がなくコード6が存在 | 6 |
| 4 | 冷房側コード2・6がなく、コード3が存在 | 3 |
| 4 | 冷房側コード2・6がなく、コード3がなく、コード4が存在 | 4 |
| 4 | 冷房側コード2・6がなく、コード3がなく、コード4がなくコード7が存在 | 7 |
| 5 | 上記以外（冷暖房要求が混在する場合を含む） | 8 |

該当するコードが存在しない場合は $tsts=8$ とする。

### 3.4 VAV負荷に基づくリセット幅

総合制御ステータスから基本リセット幅を決定する。

| $tsts$ | 状態 | $dT$ |
|:--:|----|----:|
| 1 | 冷房能力不足 | $dT_{d1}$ |
| 2 | 適正冷房 | 0 |
| 3 | 最小風量 | 冷房時: $dT_{i2}$、暖房時: $dT_{d2}$、送風時: 0 |
| 4 | 暖房適正化要求 | 0 |
| 5 | 暖房能力不足 | $dT_{i1}$ |
| 6 | 冷房適正化要求 | $dT_{d2}$ |
| 7 | 適正暖房 | $dT_{d2}$ |
| 8 | 不定 | 0 |

ここで、冷房時・暖房時・送風時はそれぞれ $CH=1$、$CH=2$、$CH=0$ を指す。

### 3.5 給気温度設定値の更新と上下限制約

給気温度設定値を更新する。

``` math
T_{sp}=preT_{sp}+dT
```

その後、上下限に制約する。

``` math
T_{smin}\leq T_{sp}\leq T_{smax}
```

## 4.  精度検証

## 5.  パラメータ同定

## 6.  Cxにおける使用場面の想定

