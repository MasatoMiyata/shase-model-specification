# 給気温度リセット制御コントローラモデル

## 1.  基本情報

| 項目 | 内容 |
|---|---|
| 名称 | 給気温度リセット制御コントローラモデル |
| 概要 | 本モデルは、複数VAVユニットの制御ステータスを一定周期で集計し、空調機の給気温度設定値をリセットする離散時間コントローラである。VAVの「能力増要求」、「適正化要求」、「最小風量」などの状態を総合して、給気温度設定値の増減幅を決定する。 |
| カテゴリ | 設備 - 空調 - 空調機 - 制御 |
| 参考文献 | loadreset_controller_v4.m |

## 2.  入出力インターフェイス

### 2.1 入力項目

#### 1) 現時刻の値
| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $Sw$ | \- | 状態フラグ | 現時刻の空調機運転スイッチ（正の値であれば運転） |
| $CH$ | \- | 整数 | 現時刻の冷暖房モード（0:送風、1:冷房、2:暖房） |
| $T_o$ | ℃ | 数値(Real) | 現時刻の外気温度 |
| $T_r$ | ℃ | 数値(Real) | 現時刻の室温 |
| $sts_i$ | \- | 整数 | 現時刻のVAVユニット$i$ の制御ステータス |

> **確認事項：状態フラグとは？「正の値」とあるので整数値？**
> **確認事項：外気温？室温？**
> **確認事項：VAVユニットの制御ステータスを算出する別のモデル（VAVユニットモデル）が必要？**

#### 2) 過去の履歴
| 変数名 | 単位 | データ型 | 初期値 | 定義 |
|:--:|----|----|----|----|
| $preSw$ | \- | 状態フラグ | 0 | 前時刻の空調機運転スイッチ |
| $preT_{sp}$ | ℃ | 数値(Real) | $T_{sp,ini}$ | 前時刻の給気温度設定値 |
| $pretsts$ | \- | 整数 | 8 | 前時刻の総合制御ステータス |
| $precount$ | sec | 数値(Real) | 0 | 前時刻の給気温度リセット周期カウンタ |

> **確認事項：初期値の列を追加してみました。確認をお願いします。**

### 2.2 出力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $T_{sp}$ | ℃ | 数値(Real) | 現時刻の給気温度設定値 |
| $dT$ | K | 数値(Real) | 現時刻の給気温度設定値リセット幅 |
| $tsts$ | \- | 整数 | 現時刻の総合制御ステータス |
| $count$ | sec | 数値(Real) | 現時刻の給気温度リセット周期カウンタ |

### 2.3 モデルパラメータ

#### 1) 入力パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $dt$ | sec | 数値(Real) | 計算周期 |
| $Nvav$ | \- | 整数 | 制御対象VAVユニットの台数|
| $t_{clr}$ | sec | 数値(Real) | 給気温度リセット制御周期 |
| $T_{smax}$ | ℃ | 数値(Real) | 給気温度設定値の上限 |
| $T_{smin}$ | ℃ | 数値(Real) | 給気温度設定値の下限 |
| $T_{sp,ini}$ | ℃ | 数値(Real) | 運転開始時の給気温度設定値 |

> **確認事項：給気温度設定値の上下限や初期値は、冷暖で異なるのでは？**

#### 2) 固定パラメータ
 
| 変数名 | 値 | 単位 | 定義 |
|:--:|----:|----|----|
| $dT_{i1}$ | 1.0 | K | 暖房能力不足時の大きい上昇幅 |
| $dT_{i2}$ | 0.5 | K | 暖房能力不足時の小さい上昇幅 |
| $dT_{d1}$ | -1.0 | K | 冷房能力不足時の大きい低下幅 |
| $dT_{d2}$ | -0.5 | K | 冷房能力不足時の小さい低下幅 |

## 3.  計算ロジック

### 3.1 空調機運転スイッチによる判定

#### a) 空調機が停止の場合 ($Sw \leq 0$)
給気温度設定値および総合制御ステータスは前時刻の値を保持する。給気温度リセット周期カウンタおよび給気温度設定値リセット幅は0とする。この場合はここで処理を終了とする。

``` math
T_{sp}=preT_{sp}, \quad dT=0, \quad tsts=pretsts,  \quad count=0
```

#### b) 空調機が運転されている場合 ($Sw > 0$)

給気温度リセット周期カウンタを更新したうえで、3.2の処理に進む。

``` math
count=precount+dt
```

ただし、運転開始時（前時刻は停止している。$preSw\leq0$）であれば、次のとおり初期化して処理を終了する。なお、給気温度リセット周期カウンタは上式で更新した値とする。

``` math
T_{sp}=T_{sp,ini}, \quad dT=0, \quad tsts=pretsts
```


### 3.2 給気温度リセット周期カウンタによる判定

#### a) 給気温度リセット周期カウンタが給気温度リセット制御周期以上になった場合 ($count \geq t_{clr}$)

給気温度リセット周期カウンタ$count$を0にリセットしたうえで3.3の処理に進む。

``` math
count=0
```

#### b) 給気温度リセット制御周期に達していない場合 ($count < t_{clr}$)

総合制御ステータスは前時刻の値を保持し、給気温度設定値リセット幅は0とする。ここで処理を終了とする。

``` math
T_{sp}=preT_{sp}, \quad dT=0, \quad tsts=pretsts
```


> **確認事項：3.1, 3.2を調整しましたが、この内容で間違いありませんでしょうか？**


### 3.3 VAVユニットの制御ステータスの集計

各VAVユニットの制御ステータスをコード別に集計する。制御ステータス0は停止としてコード8へ計上する。

| 制御コード $j$ | VAVユニットの制御ステータス |
|:--:|----|
| 1 | 冷房能力増要求 |
| 2 | 適正冷房 |
| 3 | 最小風量（能力過剰） |
| 4 | 暖房適正化要求 |
| 5 | 暖房能力増要求 |
| 6 | 冷房適正化要求 |
| 7 | 適正暖房 |
| 8 | 停止VAVの集計値、または総合状態が不定 |

> **確認事項：制御ステータスは整数で入力されるとありますが、コードとはどのような関係なのでしょうか。**
> **確認事項：「ステータス0は停止としてコード8へ計上する。」とはどういう意味でしょうか。**


制御コード $j$ のユニット台数を $Nsts(j)$ とする。総合制御ステータス $tsts$ は、次の優先順で決定する。

> **確認事項：優先順位に同じ数値が入っていますが、上から順に判定していくのでしょうか。**

| 優先 | 条件 | $tsts$ |
|:--:|----|:--:|
| 1 | 全VAVユニットが停止 | 8 |
| 2 | 制御コード1または5が存在し、$Nsts(1)>Nsts(5)$ | 1 |
| 2 | 制御コード1または5が存在し、$Nsts(1)<Nsts(5)$ | 5 |
| 2 | 制御コード1と5が同数 | 8 |
| 3 | 暖房側の制御コード4・7がなく、制御コード3が存在 | 3 |
| 3 | 暖房側の制御コード4・7がなく、制御コード3がなく、制御コード2が存在 | 2 |
| 3 | 暖房側の制御コード4・7がなく、制御コード3がなく、制御コード2がなく、制御コード6が存在 | 6 |
| 4 | 冷房側の制御コード2・6がなく、制御コード3が存在 | 3 |
| 4 | 冷房側の制御コード2・6がなく、制御コード3がなく、制御コード4が存在 | 4 |
| 4 | 冷房側の制御コード2・6がなく、制御コード3がなく、制御コード4がなく、制御コード7が存在 | 7 |
| 5 | 上記以外（冷暖房要求が混在する場合を含む） | 8 |

該当するコードが存在しない場合は $tsts=8$ とする。

> **確認事項：「全VAVユニットが停止」とは、すべてのユニットの制御コードが8であるという理解でよいでしょうか**
> **確認事項：$Nsts(1)=Nsts(5)=0$ の場合は 8 でよいですか。 制御コード1と5が同数というのは0も含まれますか？**


### 3.4 給気温度設定値リセット幅の決定

総合制御ステータスから給気温度設定値リセット幅$dT$を決定する。

| 総合制御ステータス $tsts$ | 状態 | 給気温度設定値リセット幅 $dT$ |
|:--:|----|----:|
| 1 | 冷房能力不足 | $dT_{d1}$ |
| 2 | 適正冷房 | 0 |
| 3 | 最小風量 | 冷房時($CH=1$): $dT_{i2}$<br>暖房時($CH=2$): $dT_{d2}$<br>送風時($CH=0$): 0 |
| 4 | 暖房適正化要求 | 0 |
| 5 | 暖房能力不足 | $dT_{i1}$ |
| 6 | 冷房適正化要求 | $dT_{d2}$ |
| 7 | 適正暖房 | $dT_{d2}$ |
| 8 | 不定 | 0 |


### 3.5 給気温度設定値の更新

給気温度設定値は、上下限の制約を考慮したうえで、次式で算出する。

``` math
T_{sp}=\min\left(T_{smax},\max\left(T_{smin},preT_{sp}+dT\right)\right)
```

## 4.  精度検証

## 5.  パラメータ同定

## 6.  Cxにおける使用場面の想定

