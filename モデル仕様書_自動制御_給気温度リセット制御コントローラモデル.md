# 給気温度リセット制御コントローラモデル

## 1. 基本情報

| 項目 | 内容 |
|---|---|
| 名称 | 給気温度リセット制御コントローラモデル |
| 概要 | 複数VAVユニットの制御ステータスを一定周期で集計し、空調機の給気温度設定値をリセットする離散時間コントローラである。 |
| カテゴリ | 設備 - 空調 - 空調機 - 制御 |
| 参考文献 | `loadreset_controller_v4.m` |

本モデルは、各VAVユニットの制御ステータスを入力として集約する。したがって、接続対象として `sts[i]` を出力するVAVコントローラモデルが別途必要である。VAVユニットのステータスの算出方法は、[VAV制御コントローラモデル](モデル仕様書_自動制御_VAV制御コントローラモデル.md)に従う。

## 2. 入出力インターフェイス

### 2.1 入力項目

#### 1) 現時刻の値

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $Sw$ | \- | 整数 | 空調機運転スイッチ。0は停止、1以上は運転。 |
| $CH$ | \- | 整数 | 冷暖房モード（0: 送風、1: 冷房、2: 暖房）。 |
| $sts$ | \- | 整数配列 | 全VAVユニットの制御ステータス。$sts[i]$はVAVユニット$i$のステータスであり、$i=1,\ldots,Nvav$とする。 |

#### 2) 過去の履歴（状態変数）

| 変数名 | 単位 | データ型 | 初期値 | 定義 |
|:--:|----|----|----|----|
| $preSw$ | \- | 整数 | 0 | 前時刻の空調機運転スイッチ。 |
| $preT_{sp}$ | ℃ | 数値（Real） | $T_{sp,ini}$ | 前時刻の給気温度設定値。 |
| $pretsts$ | \- | 整数 | 8 | 前時刻の総合制御ステータス。 |
| $precount$ | sec | 数値（Real） | 0 | 前時刻の給気温度リセット周期カウンタ。 |

初回計算時は上表の初期値を用いる。各時刻の処理終了後、出力を次時刻の履歴へ更新する。

``` math
preSw \leftarrow Sw,\quad preT_{sp} \leftarrow T_{sp},\quad pretsts \leftarrow tsts,\quad precount \leftarrow count
```

### 2.2 出力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $T_{sp}$ | ℃ | 数値（Real） | 現時刻の給気温度設定値。 |
| $dT$ | K | 数値（Real） | 現時刻の給気温度設定値リセット幅。 |
| $tsts$ | \- | 整数 | 全VAVの状態を集計して本コントローラが算出する総合制御ステータス。各VAVの$sts[i]$とは区別する。 |
| $count$ | sec | 数値（Real） | 現時刻の給気温度リセット周期カウンタ。 |

### 2.3 モデルパラメータ

#### 1) 入力パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $dt$ | sec | 数値（Real） | 計算周期。$dt>0$とする。 |
| $Nvav$ | \- | 整数 | 制御対象VAVユニット数。$Nvav\geq1$とし、配列$sts$の要素数と一致させる。 |
| $t_{clr}$ | sec | 数値（Real） | 給気温度リセット制御周期。$t_{clr}>0$とする。 |
| $T_{smax}$ | ℃ | 数値（Real） | 給気温度設定値の上限。 |
| $T_{smin}$ | ℃ | 数値（Real） | 給気温度設定値の下限。$T_{smin}\leq T_{smax}$とする。 |
| $T_{sp,ini}$ | ℃ | 数値（Real） | 運転開始時の給気温度設定値。$T_{smin}\leq T_{sp,ini}\leq T_{smax}$とする。 |

本仕様では、$T_{smax}$、$T_{smin}$および$T_{sp,ini}$は冷暖房モードで共通の値とする。運用上必要な場合は、冷房用および暖房用の値を個別に定義し、$CH$に応じて対応する値を選択してもよい。この場合、選択した$T_{smin}$、$T_{smax}$および$T_{sp,ini}$について、上表の上下限条件を満たすものとする。

#### 2) 固定パラメータ

| 変数名 | 値 | 単位 | 定義 |
|:--:|----:|----|----|
| $dT_{i1}$ | 1.0 | K | 暖房能力不足時の大きい上昇幅。 |
| $dT_{i2}$ | 0.5 | K | 給気温度を上昇させる小さいリセット幅。 |
| $dT_{d1}$ | -1.0 | K | 冷房能力不足時の大きい低下幅。 |
| $dT_{d2}$ | -0.5 | K | 給気温度を低下させる小さいリセット幅。 |

### 2.4 VAVユニットの制御ステータス

各$sts[i]$は接続先のVAVコントローラモデルが出力する個別ステータスである。本コントローラはこれを変更せず、3.4節で総合制御ステータス$tsts$へ集計する。

| $sts[i]$ | 名称 | 概要 |
|:--:|----|----|
| 0 | 停止 | VAVユニットが停止している状態。 |
| 1 | 冷房能力増要求 | 冷房時に能力増が必要な状態。 |
| 2 | 適正冷房 | 冷房時に他の冷房ステータスに該当しない状態。 |
| 3 | 最小風量（能力過剰） | 冷房または暖房時に最小風量近傍で能力過剰と判定された状態。 |
| 4 | 暖房適正化要求 | 暖房時の適正化要求。 |
| 5 | 暖房能力増要求 | 暖房時に能力増が必要な状態。 |
| 6 | 冷房適正化要求 | 冷房時の適正化要求。 |
| 7 | 適正暖房 | 暖房時に他の暖房ステータスに該当しない状態。 |
| 8 | 送風 | VAV比例制御が無効な送風状態。 |

## 3. 計算ロジック

### 3.1 入力値の検証

$Sw<0$、$preSw<0$、$CH\notin\{0,1,2\}$、$Nvav<1$、配列$sts$の要素数が$Nvav$と異なる、または$sts[i]\notin\{0,1,2,3,4,5,6,7,8\}$となる要素が一つでもある場合は不正入力とする。不正入力時はVAVの集計および設定値更新を行わず、次式を出力して処理を終了する。

``` math
T_{sp}=preT_{sp},\quad dT=0,\quad tsts=8,\quad count=0
```

この場合、次回の有効な運転入力を起動として扱うため、状態更新時の$preSw$は0とする。その他の履歴は3.6節に従い、出力から更新する。

### 3.2 空調機運転状態による排他的な制御フロー

以下の分岐は上から順に評価し、最初に該当した分岐だけを実行する。停止時および起動時には、通常の設定値更新（3.3～3.5節）を実行しない。

#### a) 停止時（$Sw=0$）

``` math
T_{sp}=preT_{sp},\quad dT=0,\quad tsts=pretsts,\quad count=0
```

#### b) 起動時（$Sw\geq1\land preSw=0$）

給気温度設定値を初期値へ設定し、周期カウンタを0から開始する。VAVステータスの集計および設定値リセットは行わない。

``` math
T_{sp}=T_{sp,ini},\quad dT=0,\quad tsts=pretsts,\quad count=0
```

#### c) 継続運転時（$Sw\geq1\land preSw\geq1$）

周期カウンタを更新し、3.3節に進む。

``` math
count=precount+dt
```

### 3.3 給気温度リセット周期による判定

本節は3.2節c)の継続運転時だけに適用する。

#### a) リセット周期到達時（$count\geq t_{clr}$）

周期カウンタを0にリセットし、3.4節および3.5節を実行する。

``` math
count=0
```

#### b) リセット周期未到達時（$count<t_{clr}$）

``` math
T_{sp}=preT_{sp},\quad dT=0,\quad tsts=pretsts
```

ここで処理を終了する。

### 3.4 VAVユニット制御ステータスの集計

リセット周期到達時に限り、各VAVユニットの$sts[i]$を集計する。制御コード$j$のユニット数を$Nsts(j)$とする。$sts[i]=0$の停止VAVおよび$sts[i]=8$の送風VAVは、いずれも$Nsts(8)$へ計上する。$Nsts(1)$から$Nsts(7)$は、それぞれ同じ値の$sts[i]$の台数とする。

| 集計コード $j$ | 集計対象 |
|:--:|----|
| 1～7 | $sts[i]=j$であるVAVユニット数。 |
| 8 | $sts[i]=0$である停止VAVユニット数、または$sts[i]=8$である送風VAVユニット数。 |

総合制御ステータス$tsts$は、次の表を上から順に評価して決定する。

| 優先 | 条件 | $tsts$ |
|:--:|----|:--:|
| 1 | 全VAVが停止または送風、すなわち$Nsts(8)=Nvav$ | 8 |
| 2 | $Nsts(1)>0\land Nsts(1)>Nsts(5)$ | 1 |
| 3 | $Nsts(5)>0\land Nsts(1)<Nsts(5)$ | 5 |
| 4 | $Nsts(1)>0\land Nsts(5)>0\land Nsts(1)=Nsts(5)$ | 8 |
| 5 | $Nsts(4)=0\land Nsts(7)=0\land Nsts(3)>0$ | 3 |
| 6 | $Nsts(4)=0\land Nsts(7)=0\land Nsts(3)=0\land Nsts(2)>0$ | 2 |
| 7 | $Nsts(4)=0\land Nsts(7)=0\land Nsts(3)=0\land Nsts(2)=0\land Nsts(6)>0$ | 6 |
| 8 | $Nsts(2)=0\land Nsts(6)=0\land Nsts(3)>0$ | 3 |
| 9 | $Nsts(2)=0\land Nsts(6)=0\land Nsts(3)=0\land Nsts(4)>0$ | 4 |
| 10 | $Nsts(2)=0\land Nsts(6)=0\land Nsts(3)=0\land Nsts(4)=0\land Nsts(7)>0$ | 7 |
| 11 | 上記以外（冷暖房要求の混在を含む） | 8 |

### 3.5 給気温度設定値リセット幅および設定値の更新

3.4節で求めた$tsts$および入力$CH$から、リセット幅$dT$を決定する。

| $tsts$ | 状態 | $dT$ |
|:--:|----|----:|
| 1 | 冷房能力増要求 | $dT_{d1}$ |
| 2 | 適正冷房 | 0 |
| 3 | 最小風量 | $CH=1$: $dT_{i2}$<br>$CH=2$: $dT_{d2}$<br>$CH=0$: 0 |
| 4 | 暖房適正化要求 | 0 |
| 5 | 暖房能力増要求 | $dT_{i1}$ |
| 6 | 冷房適正化要求 | $dT_{d2}$ |
| 7 | 適正暖房 | $dT_{d2}$ |
| 8 | 不定 | 0 |

``` math
T_{sp}=\min\left(T_{smax},\max\left(T_{smin},preT_{sp}+dT\right)\right)
```

### 3.6 状態更新

各時刻の処理終了後、停止、起動、継続運転およびリセット周期到達／未到達の場合は、2.1節の規則で履歴状態を更新する。不正入力の場合は、$preSw\leftarrow0$、$preT_{sp}\leftarrow T_{sp}$、$pretsts\leftarrow tsts$、$precount\leftarrow count$とする。これにより、次時刻の計算に必要な履歴は常に一意に定まる。

## 4. 代表的な入出力例

$dt=60$ sec、$t_{clr}=300$ sec、$Nvav=3$、$T_{smin}=12$ ℃、$T_{smax}=20$ ℃、$T_{sp,ini}=16$ ℃とし、初回は履歴初期値を用いる。

| 時刻 | $Sw$ | $CH$ | $sts$ | 入力時の主な履歴 | 処理経路 | $T_{sp}$ | $dT$ | $tsts$ | $count$ |
|:--:|:--:|:--:|:--:|----|----|:--:|:--:|:--:|:--:|
| 0 | 0 | 1 | [1, 1, 2] | $preT_{sp}=16$, $pretsts=8$, $precount=0$ | 停止 | 16 | 0 | 8 | 0 |
| 1 | 1 | 1 | [1, 1, 2] | $preSw=0$ | 起動 | 16 | 0 | 8 | 0 |
| 2 | 1 | 1 | [1, 1, 2] | $preSw=1$, $precount=0$ | 継続・周期未到達 | 16 | 0 | 8 | 60 |
| 3 | 1 | 1 | [1, 1, 2] | $preSw=1$, $precount=240$ | 継続・周期到達。$Nsts(1)=2>Nsts(5)=0$ | 15 | -1.0 | 1 | 0 |

時刻3では冷房能力増要求が優勢となるため、設定値は1.0 K低下する。各行の出力は3.6節に従って次行の履歴となる。

## 5. 精度検証

## 6. パラメータ同定

## 7. Cxにおける使用場面の想定
