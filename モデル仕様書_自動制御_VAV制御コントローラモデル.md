# VAVコントローラモデル

## 1. 基本情報

| 項目 | 内容 |
|---|---|
| モデルの名称 | VAVコントローラモデル |
| モデルの概要 | 室温、給気温度、室温設定値および前時刻の状態から、VAVの給気風量指令および制御ステータスを算出する離散時間コントローラである。 |
| カテゴリ | 設備 - 空調 - 制御機器（VAV） |
| 参考文献 | アズビルおよびダイキンの資料、`VAV_controler_v3.m` |

本モデルの出力$sts$は、[給気温度リセット制御コントローラモデル](モデル仕様書_自動制御_給気温度リセット制御コントローラモデル.md)へ入力できる。

## 2. 入出力インターフェイス

### 2.1 入力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $Sw$ | \- | 整数 | VAV運転スイッチ。0は停止、1以上は運転。 |
| $T_{sp}$ | ℃ | 数値（Real） | 室温設定値。 |
| $T_r$ | ℃ | 数値（Real） | 室温。 |
| $T_s$ | ℃ | 数値（Real） | 給気温度。 |
| $preSw$ | \- | 整数 | 前時刻のVAV運転スイッチ。 |
| $preCH1$ | \- | 整数 | 前時刻の冷暖房判定。 |
| $preV_d$ | $V_{max}$と同一 | 数値（Real） | 前時刻の風量指令値。 |
| $preT_r$ | ℃ | 数値（Real） | 前時刻の室温。 |

### 2.2 出力項目

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $Ss$ | \- | 整数 | 給気状態（0: 送風、1: 冷風、2: 温風）。 |
| $CH1$ | \- | 整数 | 冷暖房判定（0: 送風、1: 冷房、2: 暖房）。 |
| $CH2$ | \- | 整数 | VAV比例制御用冷暖房判定（0: 送風、1: 冷房、2: 暖房）。 |
| $V_d$ | $V_{max}$と同一 | 数値（Real） | 給気風量指令値。 |
| $sts$ | \- | 整数 | VAV制御ステータス（2.4節）。 |
| $e$ | ℃ | 数値（Real） | 室温偏差。 |
| $rV_d$ | \- | 数値（Real） | 給気風量比。 |

### 2.3 状態初期値および更新

初回計算では、次の状態初期値を用いる。$preT_r=0$は、起動時（$preSw=0$）にはPI計算で使用されないため、初期出力に影響しない。

| 状態 | 初期値 |
|:--:|----|
| $preSw$ | 0 |
| $preCH1$ | 0 |
| $preV_d$ | $V_{max}rV_{ini}$ |
| $preT_r$ | 0 ℃ |

各時刻の計算終了後、停止・送風・冷房・暖房のいずれでも次式でフィードバックする。

``` math
preSw\leftarrow Sw,\quad preCH1\leftarrow CH1,\quad preV_d\leftarrow V_d,\quad preT_r\leftarrow T_r
```

### 2.4 VAV制御ステータス

| $sts$ | 名称 | 適用状態 |
|:--:|----|----|
| 0 | 停止 | $Sw=0$ |
| 1 | 冷房能力増要求 | $CH2=1$ |
| 2 | 適正冷房 | $CH2=1$ |
| 3 | 最小風量（能力過剰） | $CH2=1$または$CH2=2$ |
| 4 | 暖房適正化要求 | $CH2=2$ |
| 5 | 暖房能力増要求 | $CH2=2$ |
| 6 | 冷房適正化要求 | $CH2=1$ |
| 7 | 適正暖房 | $CH2=2$ |
| 8 | 送風 | $Sw\geq1\land CH2=0$ |

### 2.5 パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $dT_r$ | K | 数値（Real） | 冷暖判定用ディファレンシャル。$dT_r\geq0$。 |
| $dT_s$ | K | 数値（Real） | 冷風／温風判定用ディファレンシャル。$dT_s\geq0$。 |
| $dT_i$ | K | 数値（Real） | 能力増要求／適正化要求判定用ディファレンシャル。$dT_i\geq0$。 |
| $dT_d$ | K | 数値（Real） | 最小風量／適正判定用ディファレンシャル。$dT_d\geq0$。 |
| $V_{max}$ | 任意風量単位 | 数値（Real） | VAV定格風量。$V_{max}>0$。 |
| $rV_{max}$ | \- | 数値（Real） | 最大風量比。 |
| $rV_{min}$ | \- | 数値（Real） | 最小風量比。 |
| $rV_{ini}$ | \- | 数値（Real） | 起動時および送風時の風量比。 |
| $rV_{bd}$ | \- | 数値（Real） | 適正化要求の判定に用いる境界風量比。 |
| $K_c$ | K$^{-1}$ | 数値（Real） | PI制御の比例ゲイン。 |
| $t_{step}$ | s | 数値（Real） | 制御周期。$t_{step}>0$。 |
| $t_i$ | s | 数値（Real） | 積分時間。$t_i>0$。 |

最大／最小風量近傍の判定幅は、次式で固定する。

``` math
rV_{dif}=0.05
```

風量比は次の関係を満たすものとする。これにより、3.7節の条件は相互に排他的となる。

``` math
0\leq rV_{min}<rV_{min}+rV_{dif}<rV_{bd}<rV_{max}-rV_{dif}\leq rV_{max}\leq1
```

``` math
rV_{min}\leq rV_{ini}\leq rV_{max}
```

## 3. 計算ロジック

### 3.1 停止時の処理

$Sw=0$の場合、全出力を0とする。

``` math
Ss=CH1=CH2=V_d=sts=e=rV_d=0
```

### 3.2 給気状態の判定

$Sw\geq1$の場合、次表により$Ss$を決定する。

| 条件 | $Ss$ | 給気状態 |
|----|:--:|----|
| $T_r-T_s<-dT_s$ | 2 | 温風 |
| $T_r-T_s>dT_s$ | 1 | 冷風 |
| 上記以外 | 0 | 送風 |

### 3.3 冷暖房判定

$preCH1\notin\{0,1,2\}$の場合は、有効な前時刻値を$preCH1^*=0$として扱う。その他の場合は$preCH1^*=preCH1$とする。$preCH1^*$を用いて、次表を上から順に評価する。

| $preCH1^*$ | 条件 | $CH1$ |
|:--:|----|:--:|
| 1 | $T_r<T_{sp}-dT_r$ | 0 |
| 1 | 上記以外 | 1 |
| 0 | $T_r>T_{sp}+dT_r$ | 1 |
| 0 | $T_r<T_{sp}-dT_r$ | 2 |
| 0 | 上記以外 | 1 |
| 2 | $T_r>T_{sp}+dT_r$ | 0 |
| 2 | 上記以外 | 2 |

### 3.4 VAV比例制御用冷暖房判定および室温偏差

``` math
CH2=\begin{cases}1 & (Ss=1\land CH1=1)\\2 & (Ss=2\land CH1=2)\\0 & (\mathrm{otherwise})\end{cases}
```

``` math
e=T_r-T_{sp}
```

### 3.5 送風時の処理

$Sw\geq1\land CH2=0$の場合、送風時風量を指令し、送風ステータスを出力する。

``` math
V_d=V_{max}rV_{ini},\quad rV_d=rV_{ini},\quad sts=8
```

### 3.6 冷房・暖房時の風量PI制御

$CH2\in\{1,2\}$の場合、次式により風量指令を決定する。起動時は、$rV_d$を含めて起動時風量を明示する。

``` math
preSw=0\Rightarrow V_d=V_{max}rV_{ini},\quad rV_d=rV_{ini}
```

継続運転時（$preSw\geq1$）は、以下を計算する。

``` math
K_i=\frac{K_c t_{step}}{t_i},\quad K_p=K_c-\frac{K_i}{2}
```

``` math
r_{dm}=K_p(T_r-preT_r)+K_i e
```

``` math
mode=\begin{cases}1&(CH2=1)\\-1&(CH2=2)\end{cases}
```

``` math
V_d=\min\left(V_{max}rV_{max},\max\left(V_{max}rV_{min},preV_d+V_{max}r_{dm}\,mode\right)\right)
```

``` math
rV_d=\frac{V_d}{V_{max}}
```

ここで$K_i$および$K_p$の単位はK$^{-1}$、$r_{dm}$は無次元である。

### 3.7 制御ステータスの判定

本節は$Sw\geq1\land CH2\in\{1,2\}$の場合だけに適用する。各モードの表を上から順に評価する。2.5節の風量比制約により、同一モード内で複数の条件は同時に成立しない。

#### a) 冷房時（$CH2=1$）

| 優先 | 条件 | $sts$ |
|:--:|----|:--:|
| 1 | $rV_d>rV_{max}-rV_{dif}\land T_r>T_{sp}+dT_i$ | 1 |
| 2 | $rV_d>rV_{max}-rV_{dif}\land T_r\leq T_{sp}+dT_i$ | 6 |
| 3 | $rV_{bd}<rV_d\leq rV_{max}-rV_{dif}$ | 6 |
| 4 | $rV_{min}+rV_{dif}<rV_d\leq rV_{bd}$ | 2 |
| 5 | $rV_d\leq rV_{min}+rV_{dif}\land T_r<T_{sp}-dT_d$ | 3 |
| 6 | $rV_d\leq rV_{min}+rV_{dif}\land T_r\geq T_{sp}-dT_d$ | 2 |

#### b) 暖房時（$CH2=2$）

| 優先 | 条件 | $sts$ |
|:--:|----|:--:|
| 1 | $rV_d>rV_{max}-rV_{dif}\land T_r<T_{sp}-dT_i$ | 5 |
| 2 | $rV_d>rV_{max}-rV_{dif}\land T_r\geq T_{sp}-dT_i$ | 4 |
| 3 | $rV_{bd}<rV_d\leq rV_{max}-rV_{dif}$ | 4 |
| 4 | $rV_{min}+rV_{dif}<rV_d\leq rV_{bd}$ | 7 |
| 5 | $rV_d\leq rV_{min}+rV_{dif}\land T_r>T_{sp}+dT_d$ | 3 |
| 6 | $rV_d\leq rV_{min}+rV_{dif}\land T_r\leq T_{sp}+dT_d$ | 7 |

## 4. 代表計算例

$dT_r=1$ K、$dT_s=0.2$ K、$dT_i=dT_d=0.5$ K、$V_{max}=1000$、$rV_{min}=0.2$、$rV_{ini}=0.3$、$rV_{bd}=0.7$、$rV_{max}=1.0$、$K_c=0.1$ K$^{-1}$、$t_{step}=60$ s、$t_i=300$ sとする。

| ケース | 主な入力・履歴 | $Ss$ | $CH1$ | $CH2$ | $V_d$ | $sts$ | $e$ | $rV_d$ |
|----|----|:--:|:--:|:--:|:--:|:--:|:--:|:--:|
| 停止 | $Sw=0$ | 0 | 0 | 0 | 0 | 0 | 0 | 0 |
| 起動冷房 | $Sw=1,T_r=25,T_{sp}=22,T_s=16,preSw=0,preCH1=0$ | 1 | 1 | 1 | 300 | 2 | 3 | 0.3 |
| 送風 | $Sw=1,T_r=T_{sp}=T_s=22,preCH1=0$ | 0 | 1 | 0 | 300 | 8 | 0 | 0.3 |

起動冷房では$T_r-T_s=9>dT_s$より$Ss=1$、$T_r>T_{sp}+dT_r$より$CH1=1$となる。起動時は$rV_d=rV_{ini}=0.3$であり、$rV_{min}+rV_{dif}=0.25<0.3\leq rV_{bd}$より$sts=2$となる。

## 5. 精度検証

## 6. パラメータ同定

## 7. Cxにおける使用場面の想定
