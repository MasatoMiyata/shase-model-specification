# VAVコントローラモデル

## 1.  基本情報

| 項目 | 内容 |
|---|---|
| モデルの名称 | VAVコントローラモデル |
| モデルの概要 | 本モデルは、室温・給気温度・室温設定値および前時刻の制御状態から、VAVの給気風量指令と空調状態を算出する離散時間コントローラである。給気の冷温状態と冷暖房状態の整合を確認したうえで、冷房または暖房時に速度形PI制御で風量を更新する。風量比および室温偏差に基づき、能力増要求や適正化要求などのステータスも出力する。 |
| カテゴリ | 設備 - 空調 - 制御機器（VAV） |
| 参考文献 | アズビルおよびダイキンの資料、VAV_controler_v3.m |

## 2.  入出力インターフェイス

### 2.1 入力項目

| 配列 | 変数名 | 単位 | データ型 | 定義 |
|:--:|:--:|----|----|----|
| `u(1)` | $Sw$ | \- | 状態フラグ | VAV運転スイッチ（0:停止、0以外:運転） |
| `u(2)` | $T_{sp}$ | ℃ | 数値(Real) | 室温設定値 |
| `u(3)` | $T_r$ | ℃ | 数値(Real) | 室温 |
| `u(4)` | $T_s$ | ℃ | 数値(Real) | 給気温度 |
| `u(5)` | $pre\_Sw$ | \- | 状態フラグ | 前時刻のVAV運転スイッチ |
| `u(6)` | $pre\_CH1$ | \- | 整数 | 前時刻の冷暖房判定（0:送風、1:冷房、2:暖房） |
| `u(7)` | $pre\_Vd$ | $V_{max}$と同一 | 数値(Real) | 前時刻の風量指令値 |
| `u(8)` | $pre\_T_r$ | ℃ | 数値(Real) | 前時刻の室温 |

### 2.2 出力項目

| 配列 | 変数名 | 単位 | データ型 | 定義 |
|:--:|:--:|----|----|----|
| `y(1)` | $Ss$ | \- | 整数 | 給気状態（0:送風、1:冷風、2:温風） |
| `y(2)` | $CH1$ | \- | 整数 | 冷暖房判定（0:送風、1:冷房、2:暖房） |
| `y(3)` | $CH2$ | \- | 整数 | VAV比例制御用冷暖房判定（0:送風、1:冷房、2:暖房） |
| `y(4)` | $V_d$ | $V_{max}$と同一 | 数値(Real) | 給気風量指令値 |
| `y(5)` | $sts$ | \- | 整数 | 空調状態ステータス |
| `y(6)` | $e$ | ℃ | 数値(Real) | 室温偏差 |
| `y(7)` | $rV_d$ | \- | 数値(Real) | 給気風量比 |

### 2.3 パラメータ

| 変数名 | 単位 | データ型 | 定義 |
|:--:|----|----|----|
| $dT_r$ | ℃ | 数値(Real) | 冷暖判定用ディファレンシャル |
| $dT_s$ | ℃ | 数値(Real) | 冷風／温風判定用ディファレンシャル |
| $V_{max}$ | 任意 | 数値(Real) | VAV定格風量 |
| $rV_{max}$ | \- | 数値(Real) | 最大風量比 |
| $rV_{min}$ | \- | 数値(Real) | 最小風量比 |
| $rV_{ini}$ | \- | 数値(Real) | 起動時および送風時の風量比 |
| $rV_{bd}$ | \- | 数値(Real) | 適正化要求の判定に用いる境界風量比 |
| $K_c$ | \- | 数値(Real) | PI制御の比例ゲイン |
| $t_{step}$ | s | 数値(Real) | 制御周期 |
| $t_i$ | s | 数値(Real) | 積分時間 |

内部定数として、最大・最小風量近傍の判定幅を次式で固定する。

``` math
rV_{dif}=0.05
```

## 3.  計算ロジック

### 3.1 停止時の処理

運転スイッチが0の場合、全ての状態・風量指令・室温偏差を0とする。

``` math
Sw=0
```

``` math
Ss=CH1=CH2=V_d=sts=e=rV_d=0
```

### 3.2 給気状態の判定

室温と給気温度の差から、給気状態を判定する。

| 条件 | $Ss$ | 給気状態 |
|----|:--:|----|
| $T_r-T_s<-dT_s$ | 2 | 温風 |
| $T_r-T_s>dT_s$ | 1 | 冷風 |
| 上記以外 | 0 | 送風 |

### 3.3 冷暖房判定

前時刻の冷暖房判定 $pre\_CH1$ を用いてヒステリシスを持たせ、$CH1$ を判定する。

| $pre\_CH1$ | 条件 | $CH1$ | 判定 |
|:--:|----|:--:|----|
| 1 | $T_r<T_{sp}-dT_r$ | 0 | 送風 |
| 1 | 上記以外 | 1 | 冷房 |
| 0 | $T_r>T_{sp}+dT_r$ | 1 | 冷房 |
| 0 | $T_r<T_{sp}-dT_r$ | 2 | 暖房 |
| 0 | 上記以外 | 1 | 冷房 |
| 2 | $T_r>T_{sp}+dT_r$ | 0 | 送風 |
| 2 | 上記以外 | 2 | 暖房 |

注記：$pre\_CH1=0$ かつ不感帯内では、ソースコードの実装に従い $CH1=1$（冷房）とする。コメント上は送風と記載されているが、仕様書では実装値を採用する。

### 3.4 VAV比例制御用冷暖房判定

給気状態と冷暖房判定が整合する場合のみ、VAVの比例制御を有効化する。

| 条件 | $CH2$ | 判定 |
|----|:--:|----|
| $Ss=1 \land CH1=1$ | 1 | 冷房 |
| $Ss=2 \land CH1=2$ | 2 | 暖房 |
| 上記以外 | 0 | 送風 |

室温偏差は次式とする。

``` math
e=T_r-T_{sp}
```

### 3.5 送風時の風量指令

$CH2=0$ の場合、給気風量を起動時風量に固定し、ステータスを0とする。

``` math
V_d=V_{max}rV_{ini}
```

``` math
rV_d=rV_{ini}
```

``` math
sts=0
```

### 3.6 冷房・暖房時の風量PI制御

a\) 運転開始時

前時刻の運転スイッチが停止の場合は、起動時風量を指令する。

``` math
pre\_Sw=0 \Rightarrow V_d=V_{max}rV_{ini}
```

b\) 継続運転時

以下の係数を算定する。

``` math
K_i=\frac{K_c t_{step}}{t_i}
```

``` math
K_p=K_c-\frac{K_i}{2}
```

室温偏差の増分および現在の偏差から、速度形PI制御の操作量比を算定する。

``` math
r_{dm}=K_p(T_r-pre\_T_r)+K_i e
```

冷房時は風量を増加方向、暖房時は減少方向に作用させるため、モード係数を次式とする。

``` math
mode=1 \quad (CH2=1)
```

``` math
mode=-1 \quad (CH2=2)
```

風量指令を更新する。

``` math
V_d=pre\_Vd+V_{max}r_{dm}\,mode
```

風量指令は次式の範囲に制限する。

``` math
V_{max}rV_{min}\leq V_d\leq V_{max}rV_{max}
```

風量比を算定する。

``` math
rV_d=\frac{V_d}{V_{max}}
```

### 3.7 ステータス判定

$sts$ は、冷暖房モード、風量比および室温偏差から以下のとおり判定する。

| $sts$ | 名称 | 判定概要 |
|:--:|----|----|
| 0 | 送風 | $CH2=0$ |
| 1 | 冷房能力増要求 | 冷房時、最大風量近傍 |
| 2 | 適正冷房 | 冷房時、他の冷房ステータスに該当しない場合 |
| 3 | 最小風量（能力過剰） | 冷房・暖房時、最小風量近傍で能力過剰と判定される場合 |
| 4 | 暖房適正化要求 | 暖房時、$rV_d>rV_{bd}$ または最大風量近傍 |
| 5 | 暖房能力増要求 | 暖房時、最大風量近傍 |
| 6 | 冷房適正化要求 | 冷房時、$rV_d>rV_{bd}$ または最大風量近傍 |
| 7 | 適正冷暖 | 暖房時、他の暖房ステータスに該当しない場合 |

