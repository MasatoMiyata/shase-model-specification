# 水-水熱交換器 計算仕様書（制御弁内蔵モデル）

## 0.  文書基本情報

| 項目 | 内容 |
|---|---|
| 対象コード | `HeatExchangerWaterWaterWithValveModule201504.java` |
| 対象クラス | `jp.or.ibec.best.domain.sample.air.HeatExchangerWaterWaterWithValveModule201504` |
| モデル名称 | 水-水熱交換器（制御弁内蔵・定常モデル） |
| 目的 | 熱交換器本体、二方弁・三方弁、出口温度の自己調整および最大流量自己調整を含む水-水熱交換を計算する。 |
| 計算状態 | 定常状態。熱交換器本体の蓄熱、配管熱損失および圧力損失は扱わない。 |

## 1.  基本情報

### 1.1 適用範囲・前提

- 熱交換器本体は対向流の顕熱熱交換器である。
- 入口1・入口2の温度を比較し、高温側・低温側を毎ステップ自動判定する。したがって冷却・加熱のいずれにも適用できる。
- 両側の比熱には共通定数 `WaterCoilBlock.specificHeatWater` を用いる。流量を `g/s`、比熱を `J/(g K)` として、熱容量流量を `W/K` で扱う。
- 各側には「なし」「二方弁」「三方弁」を選択できる。三方弁は本体通過流とバイパス流を混合して最終出口を算定する。
- 圧力損失、汚れ係数、伝熱性能の流量依存性は計算しない。

### 1.2 処理の流れ

1. 入力ノード、運転状態、空調モード、弁操作量および設定温度を読み込む。
2. 運転状態・空調モード・弁種別から、熱交換器本体を通過する各側流量を設定する。
3. 必要時は出口温度設定値に対して、制御側流量を二分探索する。
4. 高温側・低温側を判定して対向流熱交換計算を行う。
5. 三方弁ではバイパス流と混合し、最終出口温度・流量を更新する。
6. 出口ノード、制御流量、記録およびグラフ出力を更新する。

### 1.3 単位・記号

| 記号 | 変数名 | 単位 | 定義 |
|---|---|---|---|
| `T` | `Temp` | ℃ | 温度 |
| `m` | `FlowRate` | g/s | 質量流量 |
| `cp_w` | `WaterCoilBlock.specificHeatWater` | J/(g K) | 水の定圧比熱 |
| `C` | `ww`, `wc` | W/K | 熱容量流量（m × cp_w） |
| `U` | `spec_U` | W/(m2 K) | 熱通過率 |
| `A` | `spec_A` | m2 | 伝熱面積 |
| `UA` | `ua` | W/K | 総括伝熱性能（U × A） |
| `Q` | `q` | W | 高温側から低温側への交換熱量 |

## 2.  入出力インターフェイス

### 2.1 状態・制御入力

| ノード名 | 変数 | 単位 | 定義 |
|---|---|---|---|
| `L0_watIn1` | `watIn_1` | ℃, g/s | 入口1の水状態 |
| `L0_watIn2` | `watIn_2` | ℃, g/s | 入口2の水状態 |
| `L1_swcIn` | `swcIn` | \- | 運転状態。OFF時は停止時流量を使用する。 |
| `L1_modIn` | `modIn` | \- | 空調モード。冷房・暖房で温度制御の方向を判定する。 |
| `L0_valIn1Ctrl` | `valIn1Ctrl` | \- | 1側弁の操作量 |
| `L0_valIn2Ctrl` | `valIn2Ctrl` | \- | 2側弁の操作量 |
| `L0_valInSP_TwatOut1` | `valInSP_TwatOut1` | ℃ | 出口1の設定温度入力 |
| `L0_valInSP_TwatOut2` | `valInSP_TwatOut2` | ℃ | 出口2の設定温度入力 |

### 2.2 状態・記録出力

| ノード／記録値 | 変数 | 単位 | 定義 |
|---|---|---|---|
| `L0_watOut1` | `watOut1` | ℃, g/s | 弁・バイパス混合後の最終出口1状態 |
| `L0_watOut2` | `watOut2` | ℃, g/s | 弁・バイパス混合後の最終出口2状態 |
| `L0_valOut1CtrlFlowRate` | `valOut1CtrlFlowRate` | g/s | 1側の制御流量出力 |
| `L0_valOut2CtrlFlowRate` | `valOut2CtrlFlowRate` | g/s | 2側の制御流量出力 |
| `HEXww_交換熱量` | `q` | W | 熱交換器本体の交換熱量 |
| `watOutHex1`, `watOutHex2` | 内部状態 | ℃, g/s | 弁混合前の熱交換器本体出口状態 |

### 2.3 パラメータ

| 分類 | パラメータ | 単位 | 定義 |
|---|---|---|---|
| 熱交換器 | `spec_U`, `spec_A` | W/(m2 K), m2 | 熱通過率、伝熱面積。`UA = U × A`。既定値は `U=4000`、`A=1`。 |
| 弁（各側） | `typeValve`, `minFlowRate`, `maxFlowRate`, `offFlowRate` | - , g/s | 弁タイプ、最小・最大・停止時流量。 |
| 出口1制御 | `isSelfAdjust_TsetwatOut1`, `tLSetwatOut1`, `tUSetwatOut1` | - , ℃ | 出口1の自己調整可否、暖房時下限、冷房時上限。 |
| 出口2制御 | `isSelfAdjust_TsetwatOut2`, `tSetwatOut2` | - , ℃ | 出口2の自己調整可否、運転番号別目標温度。 |
| 外部設定値 | `isUseValInSP_TwatOut1`, `isUseValInSP_TwatOut2` | \- | 外部ノードの設定温度を使用する可否。 |
| 流量上限調整 | `isAdjust`, `numAdjustSteps` | \- | 最大流量の移動平均自己調整可否、平均ステップ数。 |

## 3.  計算ロジック

### 3.1 本体通過流量の設定

運転OFF時は、各側の本体通過流量を停止時流量に設定する。

``` text
m_set,i = m_off,i
```

冷房又は暖房時に、弁なしなら入口流量をそのまま使用する。弁ありかつ自己調整なしでは、以下により設定する。

``` text
m_set,i = m_min,i + (m_max,i - m_min,i) × min(1, u_i)
```

ここで `u_i` は弁操作量である。コードは `u_i` の下限を0に制限しないため、入力が負の場合は最小流量を下回り得る。自己調整対象の側は初期流量を最大流量とする。三方弁では、設定流量が入口流量を超える場合、入口流量に制限する。

### 3.2 対向流熱交換

入口温度の高い側を高温側 `H`、低い側を低温側 `C` とする。

``` text
C_H = m_H × cp_w

C_C = m_C × cp_w
```

`C_H × C_C = 0`、又は `T_H,in = T_C,in` の場合は、`Q=0`、両側の本体出口温度を入口温度と等しくする。

`C_H ≠ C_C` の場合、コードは次式を用いる。

``` text
w = (C_H × C_C) / (C_C - C_H)

ΔT_C = C_C(T_H,in - T_C,in) / {w[-1 + (C_C/C_H) exp(UA/w)]}

ΔT_H = C_H(T_H,in - T_C,in) / {w[1 - (C_H/C_C) exp(-UA/w)]}

Q = UA(ΔT_H - ΔT_C) / ln(ΔT_H/ΔT_C)
```

`C_H = C_C` の場合は、次式に切り替える。

``` text
ΔT_H = ΔT_C = (T_H,in - T_C,in) / (1 + UA/C_C)

Q = (T_H,in - T_C,in) / (1/C_C + 1/UA)
```

出口温度は以下で算定する。

``` text
T_C,out = T_C,in + ΔT_C

T_H,out = T_H,in - ΔT_H
```

### 3.3 二方弁・三方弁の出口状態

| 弁タイプ | 最終出口状態 |
|---|---|
| なし／二方弁 | `T_out = T_hex,out`、`m_out = m_hex`。 |
| 三方弁 | 本体通過流とバイパス流を混合する。`T_out = [T_hex,out × m_hex + T_in × (m_in - m_hex)] / m_in`、`m_out = m_in`。`m_in=0`では `T_out=T_in`、`m_out=0`。 |

### 3.4 出口温度の自己調整

出口1又は出口2の自己調整が有効な場合、流量を0から設定流量までの範囲で二分探索する。

| 制御目的 | 操作流量 | 終了条件 |
|---|---|---|
| 出口2目標温度（1側に弁あり、又は出口2差温度制御） | 1側 | 温度偏差が±0.01 ℃以内、最大100回、又は流量端値。 |
| 出口2目標温度（上記以外） | 2側 | 温度偏差が±0.01 ℃以内、最大100回、又は流量端値。 |
| 出口1上下限温度 | 1側 | 温度偏差が±0.01 ℃以内、最大200回、又は流量端値。 |

暖房時に入口1温度が出口1下限以下、又は冷房時に入口1温度が出口1上限以上の場合は、熱交換させず制御流量を0とする。

### 3.5 最大流量の自己調整

`isAdjust=true` のとき、各側で直近 `numAdjustSteps` ステップの要求流量の移動平均を計算する。平均が現在の最大調整流量を超えたときだけ、最大値・制御流量出力の上限・可変流量幅を拡張する。低下方向の更新は行わない。

### 3.6 診断・例外処理

- 高温側出口が高温側入口を超える、又は低温側出口が低温側入口を下回る場合、記録メッセージに異常を追記する。
- 低温側出口又は高温側出口が両入口温度を超える場合も、出口温度異常を記録する。
- 計算値のクランプ、例外送出、`STATUS`出力は実装されない。
- `UA=0`、極小温度差などに対する明示的な数値保護はない。

## 4.  精度検証

## 5.  パラメータ同定

## 6.  Cx・コミッショニングにおける使用場面

## 制約・適用上の留意事項

- 本仕様は対象Javaファイルの静的解析結果に基づく。`BestWater`、`WaterCoilBlock`、`Airswc`、`Airmod`などの依存クラスの詳細実装は対象外である。
- 対象コードのクラスコメントには2017年の機能更新記載があるが、仕様は現行ファイル中の実装を優先した。
- 動的挙動、圧力損失、外部への熱損失を必要とする用途には、別途モデル拡張が必要である。

